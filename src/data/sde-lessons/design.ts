import type { FoundationLessonMap } from "@/data/foundations/types";

export const designLessons: FoundationLessonMap = {
  "sde-systems/solid-and-design-patterns": {
    summary: "Use SOLID heuristics and design patterns to manage change, dependencies, and collaboration without ceremonial abstraction.",
    durationMinutes: 70, level: "interview", roleIds: ["sde", "fintech", "quant-dev"], prerequisites: ["sde-systems/object-oriented-design"],
    outcomes: ["Explain each SOLID principle through change pressure", "Select common patterns from a concrete design problem", "Recognize when abstraction costs exceed its value"],
    conceptMap: ["Change pressure reveals responsibilities", "Interfaces reverse unstable dependencies", "Patterns name recurring collaborations", "Tests expose substitutability and coupling"],
    keyIdea: "SOLID and patterns are diagnostic vocabulary for managing change—not rules to maximize interfaces, classes, or indirection.",
    analogy: { title: "Interchangeable workshop tools", body: "A standardized socket helps when tools truly need swapping; making every screw a custom adapter only slows the work." },
    theory: [{ heading: "SOLID as pressure tests", body: "SRP asks whether one unit owns unrelated reasons to change. OCP favors extension through stable seams. LSP requires subtypes to preserve client expectations. ISP keeps consumers from depending on unused operations. DIP points policy toward abstractions rather than volatile details." }, { heading: "Pattern families", body: "Strategy selects behavior, Factory centralizes construction, Adapter translates contracts, Decorator adds behavior, Observer broadcasts change, Command represents requests, and State makes lifecycle-dependent behavior explicit. Patterns may use functions, values, or modules—not only class hierarchies." }, { heading: "Costs and selection", body: "Start with the variation, invariant, and client. Compare the simplest direct design with the proposed seam. Abstractions add names, navigation, lifetime questions, and sometimes runtime cost; wait for credible change pressure and keep ownership visible." }],
    example: { title: "Strategy without inheritance", language: "cpp", code: "using RetryPolicy = std::function<bool(int attempt, Error)>;\nResult call(Service& service, RetryPolicy retry);", explanation: "A behavior parameter creates a narrow extension seam; a hierarchy is unnecessary when state and identity are not required." },
    failureModes: ["One interface per implementation", "LSP violations hidden by inheritance", "Service locator disguised as dependency inversion", "Observer lifetimes and reentrancy ignored", "Pattern names used instead of trade-off reasoning"],
    question: { id: "sde-pattern-notifications", type: "system-design", difficulty: "medium", timeboxMinutes: 20, prompt: "Design a notification component supporting email, SMS, push, provider failover, and new channels.", hints: ["Separate message policy from provider transport.", "Retries and fallback affect user-visible semantics."], answer: "Model a Notification request and channel-independent policy/orchestrator. Channel strategies own formatting and transport ports; provider adapters translate external APIs, while factories/config select concrete providers. A durable state machine records attempts, idempotency, rate limits, consent, and terminal outcome. Failover is policy-driven and must prevent duplicate user-visible sends. Keep observability and audit behavior in decorators/middleware where ordering is explicit.", rubric: ["Clear variation seams", "Appropriate pattern use", "Delivery/idempotency semantics", "Avoids excessive abstraction"], tags: ["solid", "patterns", "ood"] },
    flashcards: [{ front: "LSP asks?", back: "Can clients use the subtype without broken behavioral expectations?" }, { front: "DIP direction?", back: "High-level policy depends on stable abstractions; volatile details implement them." }, { front: "Strategy versus State?", back: "Strategy selects an algorithm; State changes behavior with an explicit lifecycle." }],
    revision: ["Name the change pressure", "Protect behavioral contracts", "Keep interfaces client-shaped", "Choose the smallest fitting pattern", "Account for indirection and lifetime"],
    sources: [{ label: "C++ Core Guidelines interfaces", url: "https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines.html#S-interfaces" }, { label: "Refactoring Guru design patterns catalog", url: "https://refactoring.guru/design-patterns/catalog" }]
  },
  "sde-systems/low-level-design": {
    summary: "Turn scoped requirements into objects, APIs, data structures, state machines, invariants, and extension points.",
    durationMinutes: 85, level: "interview", roleIds: ["sde", "fintech", "quant-dev"], prerequisites: ["sde-systems/object-oriented-design", "sde-systems/solid-and-design-patterns"],
    outcomes: ["Drive a low-level design from use cases and invariants", "Specify APIs, object relationships, and state transitions", "Evaluate concurrency, persistence, errors, and extensibility"],
    conceptMap: ["Requirements bound the problem", "Invariants assign ownership", "APIs expose use cases", "Scenarios validate collaborations"],
    keyIdea: "A strong LLD interview is an executable argument: requirements lead to invariants, boundaries, APIs, and traces—not a page of disconnected class boxes.",
    analogy: { title: "A mechanical blueprint", body: "Every part has a purpose, connection, tolerance, and failure behavior; the drawing is useful because a scenario can be traced through it." },
    theory: [{ heading: "Discovery and model", body: "Clarify actors, core use cases, exclusions, scale, and extension scenarios. Identify entities, values, policies, ports, events, and state machines. Assign each invariant to one owner and keep external systems behind interfaces." }, { heading: "APIs and data", body: "Write critical method signatures and return/error contracts. Choose collections and indexes from operations and complexity. Decide identity, mutability, lifetime, persistence, and transaction boundaries before decorating the model with patterns." }, { heading: "Validation", body: "Trace a happy path, invalid input, duplicate request, concurrency race, dependency failure, and extension. Discuss locking or optimistic concurrency, test seams, logging, and how a new rule changes the model. Refine diagrams as reasoning evolves." }],
    example: { title: "Rate limiter contract", language: "cpp", code: "struct Decision { bool allowed; int remaining; TimePoint retryAfter; };\nclass RateLimiter {\npublic:\n  virtual Decision consume(Key key, int cost, TimePoint now) = 0;\n};", explanation: "The contract exposes policy results without binding callers to token-bucket storage or a particular clock." },
    failureModes: ["Coding before scope and invariants", "Class-per-noun modeling", "No state-transition model", "Concurrency hand-waved as a database problem", "Extensions claimed but not traced"],
    question: { id: "sde-lld-rate-limiter", type: "system-design", difficulty: "hard", timeboxMinutes: 35, prompt: "Design a reusable rate-limiting library with token bucket and sliding-window policies.", hints: ["Separate policy math from state storage and time.", "Define concurrent consume semantics."], answer: "Define Key, Limit, Decision, and a RateLimiter interface with injected Clock. TokenBucket and SlidingWindow policies compute state transitions; a StateStore performs atomic read-update per key with expiry. Local implementation can use sharded locks; distributed storage needs a compare-and-set or script preserving the transition. Config resolution is separate and versioned. Specify burst semantics, retry-after rounding, missing config, clock behavior, cleanup, metrics, and deterministic tests. Adding a policy should not change clients or storage contracts unnecessarily.", rubric: ["Requirements and invariants", "Clean policy/storage/time boundaries", "Atomic concurrent transition", "Testability and extension trace"], tags: ["lld", "rate-limiter", "concurrency"] },
    flashcards: [{ front: "First LLD artifact?", back: "A bounded set of use cases, constraints, and invariants—not a class diagram." }, { front: "Where should an invariant live?", back: "Inside one explicit owner/transaction boundary whenever possible." }, { front: "How prove extensibility?", back: "Trace a named change and identify exactly which contracts and implementations change." }],
    revision: ["Clarify scope", "Write invariants and states", "Define critical contracts", "Trace data structures and transactions", "Exercise failures and one extension"],
    sources: [{ label: "C++ Core Guidelines design", url: "https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines.html#S-design" }, { label: "Google engineering practices", url: "https://google.github.io/eng-practices/" }]
  },
  "sde-systems/distributed-systems-foundations": {
    summary: "Reason about partial failure, clocks, replication, partitioning, consensus, retries, and coordination across machines.",
    durationMinutes: 100, level: "interview", roleIds: ["sde", "sre", "data-engineer", "fintech", "ml-engineer"], prerequisites: ["foundations/networking-fundamentals", "sde-systems/concurrency", "sde-systems/database-systems"],
    outcomes: ["Explain why remote failures and time are ambiguous", "Compare replication, quorum, consensus, and partitioning choices", "Design retry, idempotency, membership, and recovery protocols"],
    conceptMap: ["Networks create uncertain delivery", "Clocks provide imperfect order", "Replication copies state", "Consensus agrees on an ordered history"],
    keyIdea: "Distributed systems replace shared certainty with protocols: delay, loss, duplication, reordering, partitions, and independent failure are normal inputs.",
    analogy: { title: "Offices coordinating by unreliable mail", body: "Receipts can be delayed or duplicated, clocks disagree, and silence never proves whether a request or reply was lost." },
    theory: [{ heading: "Time and failure", body: "A timeout gives an unknown outcome, not proof of failure. Physical clocks drift and logical clocks express ordering without real time. Failure detectors are suspicions with trade-offs. Protocols need unique operation IDs, deadlines, retry budgets, and state reconciliation." }, { heading: "Replication and partitioning", body: "Leader-based replication simplifies ordering but has election and lag. Quorum schemes trade latency and availability for overlap; hinted handoff/read repair do not automatically create linearizability. Partition keys control distribution, locality, hot spots, and resharding cost." }, { heading: "Consensus and coordination", body: "Consensus lets non-faulty participants agree on values/order despite failures under stated assumptions. Raft/Paxos-style logs use terms/ballots, quorum commitment, and leader rules. Leases require clock assumptions; fencing tokens protect resources from stale leaders. Avoid coordination when commutative or partition-owned designs suffice." }],
    example: { title: "Ambiguous timeout", language: "text", code: "client --operation id=7--> server (commits)\nclient <-- response --X network loss\nclient times out: outcome is UNKNOWN\nretry id=7 -> server returns recorded result", explanation: "Stable operation identity turns an ambiguous retry into lookup/deduplication rather than a second effect." },
    failureModes: ["Treating timeout as transaction failure", "Wall-clock timestamps used as total order", "Retries without stable identity", "Consensus claimed without quorum/failure assumptions", "Hot partition keys"],
    question: { id: "sde-distributed-leader", type: "case", difficulty: "hard", timeboxMinutes: 25, prompt: "A former leader resumes after a pause and writes to shared storage while a new leader is active. Prevent the corruption.", hints: ["A lease can expire while a process is paused.", "Make the storage reject stale actors."], answer: "The paused process cannot rely on its local lease view. The coordinator issues monotonically increasing fencing tokens on leadership; every storage mutation includes the token, and storage rejects tokens older than the greatest accepted token. Use quorum-backed election/lease assumptions, safe term persistence, and idempotent recovery. Fencing protects the resource even if failure detection or process scheduling is delayed.", rubric: ["Identifies stale leader", "Uses monotonic fencing", "Enforces at resource", "States election/lease assumptions"], tags: ["distributed-systems", "consensus", "fencing"] },
    flashcards: [{ front: "Timeout proves?", back: "Only that the deadline passed; the remote operation may have failed or succeeded." }, { front: "Quorum intersection purpose?", back: "Ensure selected read/write groups overlap under the protocol's assumptions." }, { front: "Fencing token?", back: "Monotonic epoch checked by a resource to reject stale actors." }],
    revision: ["Assume loss, duplication, and delay", "Name ordering and clock assumptions", "Make operations idempotent", "Explain quorum/leader safety", "Plan partitioning and recovery"],
    sources: [{ label: "Raft paper", url: "https://raft.github.io/raft.pdf" }, { label: "Google SRE: distributed periodic scheduling", url: "https://sre.google/sre-book/distributed-periodic-scheduling/" }]
  },
  "sde-systems/consistency-models": {
    summary: "Choose and communicate visibility and ordering guarantees across replicas, clients, and transactions.",
    durationMinutes: 85, level: "advanced", roleIds: ["sde", "data-engineer", "fintech", "sre"], prerequisites: ["sde-systems/distributed-systems-foundations", "sde-systems/database-systems"],
    outcomes: ["Distinguish linearizable, sequential, causal, eventual, and session guarantees", "Separate distributed consistency from transaction isolation", "Select guarantees per invariant, latency, and failure requirement"],
    conceptMap: ["Operations form histories", "Real time and causality constrain order", "Replicas expose visibility guarantees", "Transactions constrain multi-operation anomalies"],
    keyIdea: "Consistency is a client-observable contract over histories; say exactly which ordering and visibility guarantee an operation needs.",
    analogy: { title: "Several editions of a shared notebook", body: "Everyone may eventually receive every edit, but stronger rules decide whether they see edits immediately, in causal order, or as one real-time sequence." },
    theory: [{ heading: "Single-operation models", body: "Linearizability makes each operation appear atomic between invocation and response and respects real time. Sequential consistency preserves each process's order but not wall-clock precedence. Causal consistency preserves cause-before-effect while allowing concurrent operations to differ. Eventual consistency promises convergence only with quiescence and compatible reconciliation." }, { heading: "Sessions and transactions", body: "Read-your-writes, monotonic reads, monotonic writes, and writes-follow-reads improve a user's session without global linearizability. Isolation levels instead constrain anomalies among transactions; serializability is not automatically strict serializability/linearizability." }, { heading: "Trade-offs and design", body: "During a network partition, a system cannot always provide both linearizable availability and partition tolerance for the same operation. PACELC also highlights latency/consistency trade-offs without partitions. Apply stronger coordination narrowly to invariants and make stale or tentative state visible where weaker guarantees are acceptable." }],
    example: { title: "Read-your-writes routing", language: "text", code: "write returns commit position 1842\nnext read carries minPosition=1842\nrouter chooses a replica caught up to >=1842 or the leader", explanation: "A session guarantee can preserve the user's expectation without requiring every unrelated read to be globally linearizable." },
    failureModes: ["Calling any replication eventually consistent", "Equating serializability with real-time ordering", "Using cache freshness language without a bound", "Global strong consistency for every operation", "Hiding conflict or stale-state semantics from clients"],
    question: { id: "sde-consistency-feed", type: "case", difficulty: "hard", timeboxMinutes: 22, prompt: "Choose consistency guarantees for profile edits, follower counts, and username claims in a social product.", hints: ["Different fields have different invariants.", "State the user experience under lag."], answer: "Username claims require a linearizable/uniqueness-enforced operation at the owning partition. A profile edit should at least provide read-your-writes to the author, with versioned conflict handling; global viewers may tolerate bounded staleness. Follower counts are derived and can be eventually consistent with an explicit freshness/error expectation, while the follow relationship itself needs idempotent durable writes. State routing, failure behavior, and reconciliation for each guarantee.", rubric: ["Per-operation guarantees", "Protects uniqueness invariant", "Uses session guarantee", "Makes staleness/reconciliation explicit"], tags: ["consistency", "replication", "distributed-systems"] },
    flashcards: [{ front: "Linearizability adds to sequential consistency?", back: "It respects real-time precedence between non-overlapping operations." }, { front: "Causal consistency preserves?", back: "Cause-before-effect ordering while allowing concurrent events to differ." }, { front: "Serializable versus strict serializable?", back: "Serializable matches some serial order; strict also respects real-time order." }],
    revision: ["Describe observable history", "Separate consistency and isolation", "Choose per invariant", "Name partition behavior", "Expose staleness and conflict policy"],
    sources: [{ label: "Jepsen consistency models", url: "https://jepsen.io/consistency/models" }, { label: "Linearizability paper", url: "https://cs.brown.edu/~mph/HerlihyW90/p463-herlihy.pdf" }]
  },
  "sde-systems/caching": {
    summary: "Design caches with explicit keys, freshness, invalidation, admission, eviction, failure, and stampede control.",
    durationMinutes: 75, level: "interview", roleIds: ["sde", "sre", "fintech", "ml-engineer", "data-engineer"], prerequisites: ["sde-systems/consistency-models", "sde-systems/backend-architecture"],
    outcomes: ["Choose cache-aside, read/write-through, write-back, or materialization patterns", "Define freshness and invalidation semantics", "Control hot keys, stampedes, eviction, and cache failure"],
    conceptMap: ["Keys define identity", "Population defines source behavior", "Freshness bounds divergence", "Eviction and admission allocate finite space"],
    keyIdea: "A cache is a partial, fallible replica; correctness depends on naming its source of truth and acceptable divergence.",
    analogy: { title: "A pantry near the kitchen", body: "It speeds common ingredients only if labels are precise, stock rotates, and cooks know what to do when shelves are empty or stale." },
    theory: [{ heading: "Patterns and correctness", body: "Cache-aside loads on miss; read-through delegates loading; write-through updates source and cache; write-back delays persistence and raises durability risk. Keys must include every dimension that changes the answer, including tenant, authorization, locale, or model version." }, { heading: "Freshness and invalidation", body: "TTL bounds age only loosely and does not guarantee immediate change visibility. Versioned keys, explicit invalidation, change streams, and refresh-ahead offer different ordering/failure trade-offs. Prevent stale overwrites with versions or compare-and-set." }, { heading: "Capacity and resilience", body: "Admission and LRU/LFU-style eviction decide who occupies space. Add request coalescing, randomized TTLs, stale-while-revalidate, hot-key replication, negative-cache caution, and per-tenant quotas. Cache failure must degrade through bounded origin load, not a retry storm." }],
    example: { title: "Versioned cache key", language: "text", code: "source row: product=p7, version=43\ncache key: tenant:t2:product:p7:v43\nmetadata points current version -> 43", explanation: "A new source version creates a new immutable entry, preventing an older fill from overwriting fresh content." },
    failureModes: ["Missing tenant/auth dimensions in keys", "Synchronized TTL stampede", "Stale fill overwrites a newer value", "Cache outage overloads origin", "Hit rate optimized while latency or correctness worsens"],
    question: { id: "sde-cache-catalog", type: "system-design", difficulty: "hard", timeboxMinutes: 25, prompt: "Design caching for a product catalog with heavy reads, price updates, and rare global promotions.", hints: ["Price freshness differs from descriptions.", "Plan for cache loss and hot products."], answer: "Split cacheable views by freshness/invariant rather than one giant object: descriptions may use longer versioned TTLs, while prices use short bounds or change-event invalidation with monotonic versions. Use tenant/region/currency/promotion dimensions in keys, request coalescing and TTL jitter, refresh-ahead for hot products, and stale serving only where policy permits. Protect origin with concurrency limits and load shedding during cache loss; observe hit rate, age, fill latency, evictions, and origin amplification.", rubric: ["Correct key and freshness model", "Stampede/hot-key controls", "Safe invalidation ordering", "Failure-mode protection"], tags: ["cache", "freshness", "system-design"] },
    flashcards: [{ front: "Cache-aside?", back: "Application reads cache, loads source on miss, then populates cache." }, { front: "TTL guarantees?", back: "An approximate age bound when clocks and population work; not immediate consistency." }, { front: "Request coalescing?", back: "Concurrent misses for one key share a single origin fill." }],
    revision: ["Name source of truth", "Specify complete keys", "Set freshness per field", "Prevent stampedes and stale overwrites", "Protect origin during cache failure"],
    sources: [{ label: "AWS Builders' Library: cache challenges", url: "https://aws.amazon.com/builders-library/caching-challenges-and-strategies/" }, { label: "RFC 9111 HTTP Caching", url: "https://www.rfc-editor.org/rfc/rfc9111" }]
  },
  "sde-systems/queues-and-event-streams": {
    summary: "Use work queues and append-only event streams with explicit delivery, ordering, replay, retention, and backpressure semantics.",
    durationMinutes: 85, level: "interview", roleIds: ["sde", "data-engineer", "sre", "fintech", "ml-engineer"], prerequisites: ["sde-systems/distributed-systems-foundations", "sde-systems/backend-architecture"],
    outcomes: ["Distinguish task queues, pub/sub, and durable logs", "Design at-least-once consumers with ordering and deduplication", "Operate lag, replay, poison messages, schemas, and retention"],
    conceptMap: ["Producers append work or facts", "Partitions define ordered lanes", "Consumers track progress", "Retention enables replay and recovery"],
    keyIdea: "Messaging moves coordination into a durable protocol; it does not eliminate duplicates, ordering limits, or failure—those become consumer design inputs.",
    analogy: { title: "Mailboxes versus an archived newspaper", body: "A queue assigns letters to workers; a log keeps editions so many readers can progress and reread independently." },
    theory: [{ heading: "Models", body: "A work queue distributes tasks among consumers; pub/sub fans messages to subscriptions; a log appends immutable records and lets consumer groups manage offsets. Choose from fan-out, replay, latency, retention, and ordering needs rather than product labels." }, { heading: "Delivery and ordering", body: "At-most-once may lose; at-least-once may duplicate; end-to-end exactly-once requires constrained transactions and idempotent effects. Ordering is normally per partition/key, so partition by the entity whose events must serialize and include sequence/version checks." }, { heading: "Operations", body: "Bound producer and consumer rates, observe lag age and throughput, evolve schemas compatibly, and define retention. Retries need jitter and budgets. Poison messages need diagnosis, quarantine, replay, and ownership; a dead-letter queue without tooling is a graveyard." }],
    example: { title: "Idempotent inbox", language: "sql", code: "BEGIN;\nINSERT INTO consumed(event_id) VALUES ($1) ON CONFLICT DO NOTHING;\n-- only apply effect when insert succeeded\nUPDATE balances SET reserved = reserved + $2 WHERE id = $3;\nCOMMIT;", explanation: "Deduplication marker and effect share a transaction, so redelivery cannot repeat the local mutation." },
    failureModes: ["Assuming broker acknowledgement equals business success", "Global ordering requirement on a partitioned stream", "Retrying poison records forever", "Unbounded lag hidden by throughput averages", "Breaking event schemas in place"],
    question: { id: "sde-events-orders", type: "system-design", difficulty: "hard", timeboxMinutes: 28, prompt: "Design an order-event pipeline feeding inventory, analytics, and customer notifications.", hints: ["Each consumer has different replay and side-effect needs.", "Choose an ordering key."], answer: "Publish immutable versioned order events through a transactional outbox, partitioned by order ID for per-order order. Separate consumer groups let inventory, analytics, and notifications progress independently. Inventory and notification handlers use inbox/deduplication plus conditional state transitions; analytics can replay retained events into new derived tables. Define compatible schemas, sequence checks, lag-age SLOs, bounded retries, poison quarantine with replay tooling, access controls for personal data, and reconciliation against the order source of truth.", rubric: ["Reliable publication", "Per-entity ordering", "Idempotent consumers", "Replay, schema, lag, and recovery"], tags: ["queues", "events", "streaming"] },
    flashcards: [{ front: "Queue versus log?", back: "A queue assigns work; a log retains ordered records for independently tracked readers and replay." }, { front: "Common ordering scope?", back: "Within one partition, usually selected by an entity key." }, { front: "End-to-end exactly once requires?", back: "A defined transaction boundary or idempotent/deduplicated external effects—not a broker flag alone." }],
    revision: ["Choose queue, pub/sub, or log", "State delivery and ordering", "Make effects idempotent", "Bound lag and retries", "Design schema, retention, and replay"],
    sources: [{ label: "Apache Kafka design", url: "https://kafka.apache.org/documentation/#design" }, { label: "AWS Builders' Library", url: "https://aws.amazon.com/builders-library/" }]
  }
};
