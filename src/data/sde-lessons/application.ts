import type { FoundationLessonMap } from "@/data/foundations/types";

export const applicationLessons: FoundationLessonMap = {
  "sde-systems/api-design": {
    summary: "Design durable service contracts with clear resources, semantics, validation, errors, security, and evolution rules.",
    durationMinutes: 70, level: "interview", roleIds: ["sde", "fintech", "ai-engineer", "ml-engineer", "data-engineer"], prerequisites: ["foundations/networking-fundamentals", "foundations/git-testing-and-debugging"],
    outcomes: ["Model resources and operations from client workflows", "Use HTTP semantics, idempotency, pagination, and errors precisely", "Evolve and secure APIs without surprising consumers"],
    conceptMap: ["Workflows reveal resources", "Methods carry protocol semantics", "Schemas and errors form the contract", "Compatibility governs evolution"],
    keyIdea: "An API is a long-lived promise to callers; optimize for predictable semantics and safe evolution before endpoint count.",
    analogy: { title: "A public building entrance", body: "Names, signs, access rules, receipts, and exception procedures must remain understandable even while the building changes behind them." },
    theory: [{ heading: "Contract and resources", body: "Start from client tasks and domain nouns, then define resource identifiers, representations, commands, and invariants. Keep transport models explicit, validate at the boundary, and distinguish absent, null, invalid, unauthorized, conflicting, and unavailable states." }, { heading: "Protocol semantics", body: "Use safe and idempotent method meanings deliberately. Conditional requests and idempotency keys make retries safer. Define status codes, structured error codes, pagination cursors, filtering, rate limits, request correlation, and deadlines rather than leaving behavior implicit." }, { heading: "Security and evolution", body: "Authenticate identities, authorize per resource and action, minimize returned fields, constrain inputs, and audit sensitive mutations. Prefer additive compatible changes; version only when semantics truly break. Publish deprecation and migration windows and test consumer contracts." }],
    example: { title: "Idempotent payment creation", language: "http", code: "POST /v1/payments\nIdempotency-Key: 88f...\n\n{\"amount\":{\"minor\":1250,\"currency\":\"INR\"},\"merchantId\":\"m_7\"}\n\n201 Created\nLocation: /v1/payments/pay_42", explanation: "The server binds the key to the authenticated caller and request fingerprint, then returns the original result for a safe retry." },
    failureModes: ["Verbs and implementation details in every URL", "Retrying non-idempotent mutations blindly", "Offset pagination over rapidly changing data", "Leaking internal exception text", "Breaking schemas without a migration contract"],
    question: { id: "sde-api-transfers", type: "system-design", difficulty: "medium", timeboxMinutes: 25, prompt: "Design an API for creating and tracking account transfers that clients may retry after timeouts.", hints: ["Separate acceptance from settlement.", "Define idempotency scope and conflicting reuse."], answer: "Use POST /transfers with an idempotency key scoped to caller, persist key plus canonical request fingerprint and response atomically, and reject reuse with different input. Return a stable transfer ID and state machine such as pending, posted, failed, or reversed; GET exposes status without implying settlement finality. Validate money/currency, authorize both intent and account access, use opaque cursors for history, structured errors, audit events, rate limits, and a webhook/polling contract with signed delivery and duplicate tolerance.", rubric: ["Safe retry semantics", "Explicit lifecycle", "Security and validation", "Evolution and observability"], tags: ["api", "idempotency", "fintech"] },
    flashcards: [{ front: "Safe versus idempotent?", back: "Safe does not request state change; idempotent repeated requests have the intended effect once." }, { front: "Cursor pagination advantage?", back: "It anchors continuation to ordering state and tolerates concurrent inserts better than offsets." }, { front: "Compatible schema change?", back: "Usually additive, optional, and handled by old consumers without semantic change." }],
    revision: ["Model caller workflows", "Specify method semantics", "Design retry behavior", "Standardize errors and pagination", "Plan auth, limits, and evolution"],
    sources: [{ label: "RFC 9110 HTTP Semantics", url: "https://www.rfc-editor.org/rfc/rfc9110" }, { label: "Microsoft REST API design", url: "https://learn.microsoft.com/en-us/azure/architecture/best-practices/api-design" }]
  },
  "sde-systems/backend-architecture": {
    summary: "Shape backend boundaries, request flows, transactions, asynchronous work, and deployment units around change and failure.",
    durationMinutes: 80, level: "interview", roleIds: ["sde", "fintech", "ai-engineer", "ml-engineer", "sre"], prerequisites: ["sde-systems/api-design", "sde-systems/database-systems"],
    outcomes: ["Trace a request through transport, domain, persistence, and dependencies", "Choose modular monolith or services from organizational and operational needs", "Design transaction, messaging, backpressure, and failure boundaries"],
    conceptMap: ["Interfaces admit work", "Domain policy owns invariants", "Persistence makes state durable", "Async boundaries absorb and propagate pressure"],
    keyIdea: "Architecture is the placement of boundaries and consequences: every split changes consistency, failure, deployment, and ownership costs.",
    analogy: { title: "A restaurant pass", body: "Orders enter once, stations own distinct work, the pass coordinates handoffs, and a backed-up station must slow intake before the whole kitchen collapses." },
    theory: [{ heading: "Layer and module boundaries", body: "Keep protocol parsing, application orchestration, domain policy, and infrastructure adapters separable. A modular monolith preserves local transactions and simple operations; services earn their cost when independent ownership, scale, isolation, or release cadence matters." }, { heading: "State transitions", body: "Put each invariant inside a clear transaction boundary. Across resources or services, use durable state machines, outbox/inbox patterns, idempotent handlers, compensation, and reconciliation rather than pretending remote calls share a database transaction." }, { heading: "Load and failure", body: "Bound queues and concurrency, propagate deadlines and cancellation, classify retryable errors, and isolate dependencies with bulkheads. Design health, logs, metrics, tracing, configuration, rollout, and rollback as part of the architecture—not after deployment." }],
    example: { title: "Transactional outbox", language: "sql", code: "BEGIN;\nUPDATE orders SET status = 'confirmed' WHERE id = $1;\nINSERT INTO outbox(id, topic, payload) VALUES ($2, 'order.confirmed', $3);\nCOMMIT;", explanation: "Business state and publication intent commit together; a relay retries the outbox and consumers deduplicate by event ID." },
    failureModes: ["Splitting services before ownership is clear", "Calling remote dependencies inside long database transactions", "Unbounded queues and thread pools", "Dual-writing database and broker", "Shared libraries that secretly couple every deployment"],
    question: { id: "sde-backend-orders", type: "system-design", difficulty: "hard", timeboxMinutes: 30, prompt: "Design the backend flow for confirming an order, reserving inventory, and notifying fulfillment when dependencies can fail.", hints: ["Name the local invariant and source of truth.", "Treat messages as at-least-once."], answer: "Accept an idempotent confirmation command and atomically transition the order plus write an outbox event. Inventory consumes with an inbox/deduplication key and performs a conditional reservation; its result advances a durable order state machine. Timeouts represent unknown outcomes, so query/reconcile rather than assuming failure. Bound worker concurrency, retry transient faults with jitter and a budget, dead-letter only with replay tooling, compensate an unrecoverable reservation, and expose lag, stuck states, and correlation IDs.", rubric: ["Clear ownership and transaction boundaries", "Reliable event publication", "Idempotent state machine", "Backpressure and operability"], tags: ["backend", "outbox", "reliability"] },
    flashcards: [{ front: "Why begin with a modular monolith?", back: "It keeps transactions and operations simple while preserving internal boundaries." }, { front: "Outbox solves?", back: "Atomic recording of business state and intent to publish an event." }, { front: "Backpressure means?", back: "Overload is signaled or bounded upstream instead of accumulating unlimited work." }],
    revision: ["Trace the request path", "Place invariant boundaries", "Justify every process split", "Make async handlers idempotent", "Bound load and design operations"],
    sources: [{ label: "AWS Builders' Library", url: "https://aws.amazon.com/builders-library/" }, { label: "Google Cloud architecture framework", url: "https://cloud.google.com/architecture/framework" }]
  },
  "sde-systems/browser-and-web-foundations": {
    summary: "Trace navigation through DNS, TLS, HTTP, parsing, rendering, JavaScript scheduling, storage, and browser security.",
    durationMinutes: 80, level: "foundation", roleIds: ["sde", "security", "devops"], prerequisites: ["foundations/networking-fundamentals", "sde-systems/api-design"],
    outcomes: ["Explain the critical path from URL to pixels", "Reason about the event loop, tasks, microtasks, and rendering", "Apply origin, storage, caching, and web security fundamentals"],
    conceptMap: ["Navigation resolves and connects", "HTTP transfers resources", "Parsers build DOM and CSSOM", "Event loop coordinates script and rendering"],
    keyIdea: "A web page is a distributed pipeline: network, parsing, script, style, layout, paint, and compositing can each block useful work.",
    analogy: { title: "A live stage production", body: "The script, set, props, actors, and lighting arrive on different schedules; the browser coordinates them while keeping the audience responsive." },
    theory: [{ heading: "Navigation and fetch", body: "A navigation resolves a name, establishes transport and TLS as needed, negotiates HTTP, follows caching and redirect rules, and streams bytes. Connection reuse, compression, priorities, cache validators, and CDNs shape latency." }, { heading: "Rendering and scheduling", body: "HTML parsing builds the DOM while CSS builds the CSSOM; style, layout, paint, and compositing turn structure into pixels. JavaScript can block parsing. Tasks run to completion, microtasks drain at checkpoints, and long main-thread work delays input and rendering." }, { heading: "Security and state", body: "The same-origin policy isolates principals; CORS selectively permits reads, not authentication. Cookies, local storage, service workers, and caches have distinct scope and risk. Defend with output encoding, CSP, CSRF controls, secure cookie attributes, TLS, dependency hygiene, and least privilege." }],
    example: { title: "Microtask ordering", language: "javascript", code: "console.log('A');\nqueueMicrotask(() => console.log('C'));\nsetTimeout(() => console.log('D'), 0);\nconsole.log('B');\n// A, B, C, D", explanation: "The current task finishes first, the microtask checkpoint runs next, and the timer callback is a later task." },
    failureModes: ["Large render-blocking bundles", "Layout reads interleaved with writes", "Treating CORS as authentication", "Long main-thread tasks", "Caching personalized responses publicly"],
    question: { id: "sde-browser-slow-page", type: "case", difficulty: "medium", timeboxMinutes: 18, prompt: "A page has a fast API but slow Largest Contentful Paint and delayed clicks. Diagnose the browser path.", hints: ["API time is only one segment.", "Inspect the main thread and critical resources."], answer: "Use a network waterfall and performance trace to separate DNS/connect/TLS/TTFB, resource discovery, transfer, parsing, style/layout/paint, and long tasks. Check render-blocking CSS/fonts, image dimensions/priority, hydration and bundle cost, synchronous third-party scripts, repeated layout, and microtask floods. Optimize the actual LCP resource and split/defer CPU work, then verify field metrics and cache behavior across devices.", rubric: ["End-to-end critical path", "Main-thread diagnosis", "Resource prioritization", "Measurement after changes"], tags: ["browser", "performance", "web"] },
    flashcards: [{ front: "DOM plus CSSOM produce?", back: "Render/style information used for layout and painting." }, { front: "Microtasks run when?", back: "At checkpoints after current script/task work, before the next task and commonly before rendering." }, { front: "CORS purpose?", back: "Controls whether browser scripts may read cross-origin responses; it is not user authorization." }],
    revision: ["Trace URL to response", "Trace bytes to pixels", "Know task/microtask order", "Separate origin, auth, and CORS", "Profile network and main thread"],
    sources: [{ label: "MDN How browsers work", url: "https://developer.mozilla.org/en-US/docs/Web/Performance/Guides/How_browsers_work" }, { label: "HTML event loops", url: "https://html.spec.whatwg.org/multipage/webappapis.html#event-loops" }]
  },
  "sde-systems/mobile-foundations": {
    summary: "Design mobile clients around lifecycle, constrained resources, local state, synchronization, platform boundaries, and safe releases.",
    durationMinutes: 65, level: "foundation", roleIds: ["sde", "fintech", "security"], prerequisites: ["sde-systems/api-design", "foundations/git-testing-and-debugging"],
    outcomes: ["Model UI, domain, data, and platform boundaries", "Design offline-capable synchronization and lifecycle-safe work", "Reason about battery, network, storage, security, and release constraints"],
    conceptMap: ["Lifecycle interrupts execution", "Local state preserves continuity", "Sync reconciles remote truth", "Platform services enforce device constraints"],
    keyIdea: "A mobile app is an occasionally connected replicated client running under an operating system that can pause or kill it at any time.",
    analogy: { title: "A field notebook that later syncs", body: "Work must remain useful without headquarters, record what changed, and reconcile safely when contact returns." },
    theory: [{ heading: "Architecture and lifecycle", body: "Separate presentation state, domain use cases, repositories, and platform adapters. Make view state restorable and long work lifecycle-aware. Background execution is budgeted and scheduled by the OS, so persist intent before relying on callbacks." }, { heading: "Data and sync", body: "Treat local storage as a deliberate cache or source of pending work. Track stable IDs, versions, tombstones, retry state, and conflict policy. Idempotent APIs, incremental sync, optimistic UI, and visible pending/error states turn intermittent networks into defined behavior." }, { heading: "Device and delivery", body: "Budget CPU, memory, radios, disk, and startup. Store secrets in platform-protected facilities, minimize sensitive data, validate deep links, and pin trust only with an update strategy. Use staged rollout, crash/ANR metrics, feature flags, and backward-compatible server contracts." }],
    example: { title: "Durable offline command", language: "text", code: "UI action -> local transaction: update view + enqueue command\nworker -> send(commandId, version)\nserver -> deduplicate, validate version, return canonical state\nclient -> mark complete or expose conflict", explanation: "The user's intent survives process death and retries; reconciliation replaces optimistic state with canonical state." },
    failureModes: ["Keeping critical state only in memory", "Retry loops that ignore connectivity and battery", "Last-write-wins without domain justification", "Shipping a client that requires an immediate server upgrade", "Logging tokens or personal data"],
    question: { id: "sde-mobile-expenses", type: "system-design", difficulty: "medium", timeboxMinutes: 22, prompt: "Design offline expense creation and later synchronization for a mobile app.", hints: ["Persist user intent atomically.", "Define edit conflicts and attachment behavior."], answer: "Create a client-generated stable expense ID and atomically store the draft plus an outbox command. UI shows pending/failed states. A constrained background worker uploads metadata idempotently, uses resumable checksummed attachment upload, then records the canonical server version. Use optimistic concurrency for edits, field/domain-aware conflict UI rather than silent overwrite, tombstones for deletion, encrypted local storage for sensitive data, bounded retries, and backward-compatible schemas.", rubric: ["Durable offline state", "Idempotent sync", "Conflict policy", "Resource/security awareness"], tags: ["mobile", "offline", "sync"] },
    flashcards: [{ front: "Why client-generated IDs?", back: "They give offline work a stable identity and support idempotent synchronization." }, { front: "Tombstone purpose?", back: "Represent deletion long enough for replicas to learn it." }, { front: "Optimistic UI needs?", back: "Visible pending/error state and reconciliation with canonical results." }],
    revision: ["Assume process death", "Persist intent before work", "Define sync identity and versions", "Budget device resources", "Roll out compatibly and observe"],
    sources: [{ label: "Android app architecture", url: "https://developer.android.com/topic/architecture" }, { label: "Apple app architecture", url: "https://developer.apple.com/documentation/swiftui/model-data" }]
  },
  "sde-systems/testing-strategy": {
    summary: "Build a risk-driven test portfolio across units, integrations, contracts, systems, properties, and production signals.",
    durationMinutes: 70, level: "interview", roleIds: ["sde", "sre", "devops", "fintech", "security"], prerequisites: ["foundations/git-testing-and-debugging", "sde-systems/backend-architecture"],
    outcomes: ["Select test scope from risk and feedback needs", "Create deterministic seams without testing implementation trivia", "Design contract, migration, concurrency, and resilience tests"],
    conceptMap: ["Risk selects evidence", "Small tests localize feedback", "Boundary tests verify contracts", "Production signals test assumptions continuously"],
    keyIdea: "Testing is an evidence portfolio: each layer should catch a valuable failure at the lowest faithful cost.",
    analogy: { title: "Layered safety inspection", body: "Parts are measured alone, assemblies under load, interfaces at handoff, and the whole machine in realistic operation." },
    theory: [{ heading: "Portfolio design", body: "Unit tests exercise decisions quickly; integration tests verify real boundaries; contract tests protect provider-consumer schemas; end-to-end tests cover a few critical journeys. Property, fuzz, performance, migration, security, and chaos tests address risks examples miss." }, { heading: "Testability and determinism", body: "Inject clocks, randomness, IDs, and external ports. Prefer observable behavior over private method assertions. Use fakes for controlled domain collaborators and real ephemeral infrastructure when compatibility matters; avoid mocks that merely replay implementation." }, { heading: "Operations and quality", body: "Treat flaky tests as defects. Parallelize independent suites, quarantine only with ownership and expiry, and measure mutation or escaped-defect signals rather than coverage alone. Canary checks, synthetic probes, and telemetry extend verification into real environments." }],
    example: { title: "Property for ledger conservation", language: "typescript", code: "forAll(validPostingBatch, batch => {\n  const result = post(batch);\n  expect(sumDebits(result)).toEqual(sumCredits(result));\n});", explanation: "Generators explore many valid shapes while the invariant remains stable; example tests still document important cases." },
    failureModes: ["A brittle end-to-end-heavy pyramid", "Mocking every collaborator interaction", "Coverage target without assertion quality", "Shared mutable test data", "Ignoring flaky or slow tests"],
    question: { id: "sde-testing-pipeline", type: "case", difficulty: "medium", timeboxMinutes: 20, prompt: "Create a test strategy for a payment API migrating its database schema with zero downtime.", hints: ["Cover old and new application versions.", "Rollback is part of the contract."], answer: "Unit-test validation and money invariants; integration-test transactions against the real database; contract-test API and event schemas; and run concurrency/idempotency properties. For expand-migrate-contract, verify old app/new schema and new app/expanded schema, dual-read/write behavior if used, resumable backfill, checksums/counts, rollback before destructive contraction, and load impact. Keep a small end-to-end authorization-to-settlement path, security tests, canary metrics, and synthetic probes.", rubric: ["Risk-based layers", "Real database compatibility", "Migration/rollback matrix", "Production verification"], tags: ["testing", "migration", "payments"] },
    flashcards: [{ front: "Contract test verifies?", back: "A provider-consumer interface independently of a full end-to-end environment." }, { front: "Property-based testing?", back: "Generates many inputs to check stable invariants rather than only examples." }, { front: "Why not coverage alone?", back: "Execution does not prove meaningful assertions or important behavior." }],
    revision: ["Map risks to test types", "Test behavior at the lowest faithful layer", "Control time/randomness/dependencies", "Include migration and failure", "Observe canaries and production"],
    sources: [{ label: "Google testing blog", url: "https://testing.googleblog.com/" }, { label: "Google engineering practices: testing", url: "https://google.github.io/eng-practices/review/developer/testing.html" }]
  },
  "sde-systems/performance-engineering": {
    summary: "Measure and improve latency, throughput, utilization, allocation, I/O, and scalability with controlled experiments.",
    durationMinutes: 80, level: "interview", roleIds: ["sde", "sre", "quant-dev", "ml-engineer", "fintech"], prerequisites: ["foundations/complexity-analysis", "sde-systems/operating-systems", "sde-systems/testing-strategy"],
    outcomes: ["Define workload and percentile objectives", "Use profiling, tracing, benchmarks, and load tests correctly", "Connect queueing, contention, locality, and resource saturation to tail latency"],
    conceptMap: ["Workload drives demand", "Resources impose service capacity", "Queues amplify waiting", "Measurement locates dominant cost"],
    keyIdea: "Performance work is experimental science: define the workload and objective, measure the bottleneck, change one cause, and verify the distribution.",
    analogy: { title: "Traffic through toll booths", body: "Average drive time hides the holiday queue; utilization near capacity makes small arrival bursts create long tail waits." },
    theory: [{ heading: "Metrics and queueing", body: "Latency distributions need percentiles and coordinated-omission-aware measurement; throughput without error and saturation is incomplete. Little's Law relates concurrency, throughput, and time in stable systems. As utilization approaches capacity, queue delay and tails often rise nonlinearly." }, { heading: "Measurement tools", body: "Profiles attribute CPU, allocation, lock, and I/O cost; tracing follows critical paths; hardware counters expose cache and branch behavior. Microbenchmarks isolate mechanisms but need warm-up, stable inputs, multiple samples, and protection from compiler elimination." }, { heading: "Optimization", body: "First remove unnecessary work and choose a better algorithm or data layout. Then reduce copies, allocations, contention, round trips, and serialization. Load-test realistic mixes through steady state and overload, compare against a baseline, and protect readability with evidence and regression tests." }],
    example: { title: "Little's Law estimate", language: "text", code: "arrival rate = 800 requests/s\naverage in-system time = 0.125 s\nexpected concurrency L = lambda * W = 100 requests", explanation: "The estimate sizes in-flight work at steady state; burst tolerance and tail objectives still require headroom." },
    failureModes: ["Optimizing averages while tails regress", "Microbenchmarking unrealistic hot data", "Load generators that omit client wait time", "Adding concurrency past saturation", "Changing multiple variables without a baseline"],
    question: { id: "sde-performance-p99", type: "case", difficulty: "hard", timeboxMinutes: 20, prompt: "Median latency is 40 ms but p99 jumps from 200 ms to 3 s at 70% CPU. How do you investigate?", hints: ["CPU average can hide a saturated shard or serialized resource.", "Correlate queue time with service time."], answer: "Reproduce the realistic arrival pattern and validate client-side percentile measurement. Break traces into queue and service time; correlate tails with per-core CPU, run queue, locks, pools, dependency latency, GC/allocation, I/O, cache misses, and hot keys. Inspect saturation by shard and bounded-resource occupancy. Use profiles during the tail window, test one hypothesis at a time, add admission control or remove the bottleneck, and rerun steady-state plus overload tests against an unchanged baseline.", rubric: ["Validates measurement", "Separates queue/service time", "Finds localized saturation", "Controlled fix and retest"], tags: ["performance", "p99", "profiling"] },
    flashcards: [{ front: "Little's Law?", back: "In stable systems, average concurrency equals throughput times average time in system." }, { front: "Why percentile latency?", back: "Averages hide the slow requests users and dependencies experience." }, { front: "First optimization targets?", back: "Unnecessary work, algorithms, data layout, round trips, and contention." }],
    revision: ["Define workload and SLO", "Measure distribution and saturation", "Profile before editing", "Change one cause", "Retest steady state and overload"],
    sources: [{ label: "Google SRE book: monitoring distributed systems", url: "https://sre.google/sre-book/monitoring-distributed-systems/" }, { label: "Linux perf documentation", url: "https://perf.wiki.kernel.org/index.php/Main_Page" }]
  }
};
