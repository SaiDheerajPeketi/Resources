import type { FoundationLessonMap } from "@/data/foundations/types";

export const coreCsLessons: FoundationLessonMap = {
  "sde-systems/object-oriented-design": {
    summary: "Model responsibilities, invariants, collaborations, and change boundaries without turning every noun into a class.",
    durationMinutes: 70, level: "foundation", roleIds: ["sde", "ai-engineer", "ml-engineer", "fintech", "quant-dev"], prerequisites: ["foundations/c-plus-plus-interview-setup"],
    outcomes: ["Identify entities, value objects, services, and policies", "Assign behavior by responsibility and invariant ownership", "Explain composition, inheritance, coupling, and test seams"],
    conceptMap: ["Use cases expose behavior", "Invariants define ownership", "Objects collaborate through narrow contracts", "Change pressure tests the boundaries"],
    keyIdea: "Good object design localizes reasons to change; it is not measured by class count or pattern vocabulary.",
    analogy: { title: "A workshop with accountable stations", body: "Each station owns a clear transformation and safety rule, then passes a well-defined artifact onward." },
    theory: [{ heading: "Model from behavior", body: "Start with use cases, commands, events, and invariants. Entities carry identity across change; value objects are defined by immutable values; services coordinate behavior that belongs to no single entity." }, { heading: "Boundaries and collaboration", body: "Encapsulation protects invariants. Prefer composition for replaceable behavior and inheritance only for a genuine substitutable abstraction. Dependencies should point toward stable policies through small interfaces." }, { heading: "Interview method", body: "Clarify scope, narrate objects and interactions, write critical APIs, trace one happy path and one failure, then test extension points, concurrency, persistence, and observability. Avoid speculative frameworks." }],
    example: { title: "Policy separated from state", language: "cpp", code: "class PricingPolicy { public: virtual Money price(const Cart&) const = 0; };\nclass Checkout {\n  const PricingPolicy& pricing;\npublic:\n  Receipt place(const Cart& cart, PaymentPort& payments);\n};", explanation: "Checkout owns orchestration while pricing and external payment behavior remain replaceable contracts." },
    failureModes: ["Anemic data bags with one god service", "Deep inheritance hierarchies", "Interfaces for every class", "Patterns before requirements", "Ignoring invariants and failure behavior"],
    question: { id: "sde-ood-parking", type: "system-design", difficulty: "medium", timeboxMinutes: 25, prompt: "Design the core objects for a multi-level parking garage with vehicle-size rules, tickets, pricing, and exits.", hints: ["Separate spot allocation from pricing.", "State the invariants around occupancy and ticket lifecycle."], answer: "Model Garage/Level/Spot identities, an immutable Vehicle descriptor, Ticket lifecycle, an AllocationPolicy, PricingPolicy, Gate/Payment ports, and a transaction-scoped ParkingService. The spot owns occupancy invariants; allocation selects compatible free spots; ticket states prevent double exit. Trace entry, full garage, lost ticket, payment failure, and concurrent claims; persist atomic spot/ticket changes and expose audit events.", rubric: ["Clear responsibilities", "Explicit invariants", "Extensible policies", "Failure and concurrency reasoning"], tags: ["ood", "lld", "invariants"] },
    flashcards: [{ front: "Entity versus value object?", back: "Entity has continuing identity; value object is defined by its values." }, { front: "Composition over inheritance means?", back: "Assemble behavior through owned collaborators unless subtype substitution is real." }, { front: "Best class boundary test?", back: "Does it localize an invariant and a reason to change?" }],
    revision: ["Start from use cases", "Name invariants", "Assign responsibility", "Prefer narrow contracts", "Trace success, failure, extension"],
    sources: [{ label: "C++ Core Guidelines class design", url: "https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines.html#S-class" }, { label: "Microsoft API design guidance", url: "https://learn.microsoft.com/en-us/azure/architecture/best-practices/api-design" }]
  },
  "sde-systems/modern-c-plus-plus-internals": {
    summary: "Reason about value categories, moves, templates, object layout, dispatch, and zero-cost abstractions in modern C++.",
    durationMinutes: 85, level: "interview", roleIds: ["sde", "quant-dev", "fintech", "ml-engineer"], prerequisites: ["foundations/c-plus-plus-interview-setup", "foundations/stl-and-iterators"],
    outcomes: ["Explain copy/move construction and value categories", "Reason about virtual dispatch, templates, and object representation", "Apply C++20/23 features without weakening lifetime safety"],
    conceptMap: ["Types encode ownership and operations", "Value category guides overload resolution", "Templates move work and checks to compile time", "ABI and layout connect abstractions to machine cost"],
    keyIdea: "Modern C++ performance comes from explicit ownership plus abstractions the compiler can erase—not from manually avoiding every object.",
    analogy: { title: "A shipping label for resources", body: "Copy duplicates a shipment, move transfers its label, and a reference only points at where it currently lives." },
    theory: [{ heading: "Values and special members", body: "lvalues identify persistent objects; prvalues produce values; xvalues permit resource reuse. The rule of zero delegates ownership to standard types. A moved-from object remains valid but usually has unspecified state." }, { heading: "Dispatch and representation", body: "Virtual calls usually indirect through a vtable and require a polymorphic base destructor for deletion through base. Object size reflects members, padding, alignment, and possible vptrs; layout is implementation/ABI territory, so measure instead of assuming." }, { heading: "Templates and modern facilities", body: "Templates instantiate types and enable static polymorphism; concepts constrain requirements clearly. C++20 ranges, spans, designated initialization limits, and coroutines improve expression but do not relax dangling, allocation, or synchronization rules." }],
    example: { title: "Rule-of-zero movable owner", language: "cpp", code: "class Buffer {\n  std::vector<std::byte> bytes_;\npublic:\n  explicit Buffer(std::size_t n) : bytes_(n) {}\n  std::span<std::byte> view() noexcept { return bytes_; }\n};", explanation: "vector owns the resource; generated copy/move/destructor behavior is correct, while span is a non-owning lifetime-bound view." },
    failureModes: ["Returning a view to destroyed storage", "Using std::move as a move guarantee", "Deleting through a non-virtual polymorphic base", "Assuming object layout", "Template abstractions with hidden code-size cost"],
    question: { id: "sde-cpp-move", type: "short", difficulty: "hard", timeboxMinutes: 15, prompt: "What does std::move do, when can a vector relocation use move, and why does noexcept matter?", hints: ["std::move is a cast.", "A container must preserve strong exception guarantees."], answer: "std::move casts its argument to an xvalue so overload resolution may select a move operation; it transfers nothing itself. During reallocation, vector can move elements when the move constructor is noexcept (or copying is impossible). If move might throw and copy exists, copying can preserve the strong guarantee because old elements remain intact. The moved-from state must still be valid.", rubric: ["Cast not operation", "Relocation explanation", "noexcept/strong guarantee", "Valid moved-from state"], tags: ["c++", "move", "internals"] },
    flashcards: [{ front: "std::move does?", back: "Casts to an xvalue; a later operation may move." }, { front: "Rule of zero?", back: "Use owning members so compiler-generated special members are correct." }, { front: "span owns data?", back: "No; its validity is bounded by the referenced storage lifetime." }],
    revision: ["Know value categories", "Prefer rule of zero", "Mark valid moves noexcept", "Use virtual destructor for polymorphic deletion", "Measure ABI/layout/code size"],
    sources: [{ label: "cppreference value categories", url: "https://en.cppreference.com/w/cpp/language/value_category" }, { label: "C++ Core Guidelines", url: "https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines.html" }]
  },
  "sde-systems/memory-management": {
    summary: "Connect storage, lifetime, ownership, allocation, virtual memory, caches, and memory-safe C++ design.",
    durationMinutes: 80, level: "interview", roleIds: ["sde", "quant-dev", "fintech", "ml-engineer"], prerequisites: ["sde-systems/modern-c-plus-plus-internals", "foundations/c-plus-plus-interview-setup"],
    outcomes: ["Distinguish storage duration, object lifetime, and ownership", "Use RAII and smart pointers correctly", "Diagnose leaks, dangling access, fragmentation, and locality"],
    conceptMap: ["Storage supplies bytes", "Construction begins object lifetime", "Ownership determines destruction", "Access pattern determines cache and paging cost"],
    keyIdea: "An address can still exist after an object's lifetime ends; memory safety follows lifetime and ownership, not whether a pointer is non-null.",
    analogy: { title: "Hotel room versus guest", body: "The room's bytes may remain, but using them as if the previous guest still occupies the room violates the lifetime contract." },
    theory: [{ heading: "Lifetime and ownership", body: "Automatic, static, thread, and dynamic storage durations differ from object lifetime. RAII couples acquisition to construction and release to destruction. unique_ptr expresses sole ownership; shared_ptr expresses shared lifetime, not shared thread safety; weak_ptr breaks cycles." }, { heading: "Allocation and locality", body: "Allocators manage blocks and metadata, with fragmentation and synchronization cost. Pools/arenas help homogeneous or phase-based lifetimes. Contiguous layouts improve spatial locality and prefetching; pointer-rich structures trade locality for update flexibility." }, { heading: "Virtual memory and debugging", body: "Processes use virtual addresses mapped to pages; faults bring pages into memory and TLBs cache translations. Use sanitizers, heap profiles, allocation traces, and resident-set observations. Fix ownership first rather than masking symptoms with a pool." }],
    example: { title: "Acyclic shared ownership", language: "cpp", code: "struct Node {\n  std::vector<std::shared_ptr<Node>> children;\n  std::weak_ptr<Node> parent;\n};", explanation: "Children keep descendants alive; the non-owning parent reference avoids a reference-count cycle." },
    failureModes: ["shared_ptr everywhere", "Views outliving owners", "Reference cycles", "Allocator mismatch", "Optimizing allocations while ignoring locality"],
    question: { id: "sde-memory-growth", type: "case", difficulty: "hard", timeboxMinutes: 18, prompt: "A C++ service's RSS grows while leak sanitizer reports no leaks. Investigate.", hints: ["Allocated-but-reachable is not a leak.", "Allocator retention and caches affect RSS."], answer: "Separate live heap, allocator-reserved memory, mapped files, stacks, and page cache. Compare heap profiles over time, inspect unbounded caches/queues and shared_ptr retention, allocator arenas/fragmentation, thread count, and large transient peaks. Trigger controlled load/idle cycles and review RSS versus allocated bytes. Bound caches/queues, reduce arenas or choose an allocator policy only after identifying retention.", rubric: ["Distinguishes RSS and live heap", "Checks reachable retention", "Checks allocator/thread behavior", "Evidence-led remediation"], tags: ["memory", "cpp", "debugging"] },
    flashcards: [{ front: "Storage duration versus lifetime?", back: "Storage supplies bytes; lifetime defines when an object exists in them." }, { front: "shared_ptr guarantees?", back: "Thread-safe control-block counting, not synchronized object access." }, { front: "RSS includes?", back: "Resident heap, stacks, mappings, and other resident pages—not only live objects." }],
    revision: ["Name the owner", "Separate view from ownership", "Break cycles", "Profile live and reserved bytes", "Optimize layout before allocator tricks"],
    sources: [{ label: "cppreference object lifetime", url: "https://en.cppreference.com/w/cpp/language/lifetime" }, { label: "C++ Core Guidelines resource management", url: "https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines.html#S-resource" }]
  },
  "sde-systems/concurrency": {
    summary: "Design correct concurrent code with happens-before reasoning, synchronization, atomics, task structures, and contention-aware measurement.",
    durationMinutes: 90, level: "interview", roleIds: ["sde", "quant-dev", "fintech", "sre", "ml-engineer"], prerequisites: ["sde-systems/memory-management", "foundations/graphs"],
    outcomes: ["Identify races, deadlocks, starvation, and ordering requirements", "Use mutexes, condition variables, and atomics correctly", "Choose threads, tasks, actors, or message passing from workload constraints"],
    conceptMap: ["Threads interleave operations", "Synchronization establishes happens-before", "Invariants define critical sections", "Contention and scheduling determine scalability"],
    keyIdea: "A data race in C++ is undefined behavior; correctness requires an ordering proof, not a timing story that usually works.",
    analogy: { title: "A shared whiteboard with handoff receipts", body: "A lock or release/acquire handoff proves when one worker's completed notes become visible to another." },
    theory: [{ heading: "Correctness", body: "Conflicting unsynchronized accesses with at least one write form a data race. Mutex unlock/lock and release/acquire atomics can establish happens-before. Protect invariants, not individual fields, and use condition-variable predicates in loops." }, { heading: "Liveness", body: "Deadlock needs cyclic waiting; prevent with global lock order, scoped locking, or less shared state. Starvation and priority inversion are distinct. Cancellation, shutdown, exceptions, and queue closure need explicit protocols." }, { heading: "Performance", body: "Amdahl's law bounds parallel speedup. Measure contention, runnable threads, context switches, queue delay, cache-line bouncing, and false sharing. Task pools and message passing can simplify ownership but still require backpressure." }],
    example: { title: "Predicate-based wait", language: "cpp", code: "std::unique_lock lock(mu);\nready.wait(lock, [&] { return closed || !queue.empty(); });\nif (queue.empty()) return std::nullopt;\nauto item = std::move(queue.front());\nqueue.pop();", explanation: "The predicate handles spurious wakeups and the closed state makes shutdown observable." },
    failureModes: ["Checking condition outside the lock", "Detached threads with borrowed state", "Inconsistent lock order", "Relaxed atomics without proof", "Unbounded worker queues"],
    question: { id: "sde-concurrency-cache", type: "coding", difficulty: "hard", timeboxMinutes: 30, prompt: "Design a thread-safe bounded cache with concurrent reads and atomic replacement.", hints: ["State the consistency promise.", "Eviction and value lifetime must share an invariant."], answer: "Define linearizable get/put or weaker semantics first. Use a mutex/shared_mutex around map plus eviction metadata, returning value copies or shared ownership so eviction cannot dangle callers. Put updates value and recency atomically, evicts under the same invariant, and enforces capacity. Avoid callbacks under locks, define shutdown, and benchmark read/write mix; sharding can reduce contention if cross-shard capacity becomes approximate.", rubric: ["Consistency contract", "Lifetime-safe API", "Atomic map/eviction invariant", "Contention and shutdown reasoning"], tags: ["concurrency", "cache", "cpp"] },
    flashcards: [{ front: "Data race in C++?", back: "Conflicting unsynchronized accesses, at least one write; behavior is undefined." }, { front: "Condition variable rule?", back: "Wait in a predicate loop while holding the associated lock." }, { front: "False sharing?", back: "Independent hot data shares a cache line and causes coherence traffic." }],
    revision: ["Write invariant and ordering", "Use scoped locks", "Predicate every wait", "Define shutdown/backpressure", "Measure contention and cache effects"],
    sources: [{ label: "cppreference multithreaded executions", url: "https://en.cppreference.com/w/cpp/language/multithread" }, { label: "C++ memory model", url: "https://en.cppreference.com/w/cpp/language/memory_model" }]
  },
  "sde-systems/operating-systems": {
    summary: "Understand processes, threads, scheduling, virtual memory, files, I/O, and the kernel boundaries behind application behavior.",
    durationMinutes: 90, level: "interview", roleIds: ["sde", "sre", "devops", "security", "quant-dev"], prerequisites: ["foundations/linux-and-the-cli", "sde-systems/concurrency"],
    outcomes: ["Explain process, thread, syscall, and scheduling transitions", "Trace virtual memory and page faults", "Reason about file descriptors, buffering, I/O, and crash behavior"],
    conceptMap: ["Kernel isolates and schedules execution", "Virtual memory maps addresses to pages", "File descriptors name kernel resources", "I/O completion and buffering shape latency"],
    keyIdea: "The OS virtualizes CPU, memory, and devices while enforcing isolation; performance problems often surface where those abstractions meet finite hardware.",
    analogy: { title: "A city scheduler and address translator", body: "The kernel assigns road time, translates private street maps, and controls access to shared utilities." },
    theory: [{ heading: "Execution", body: "A process owns an address space and resources; threads share that space but have separate stacks/registers. Syscalls cross into the kernel. Schedulers balance fairness, priority, affinity, and throughput; context switches cost state and cache locality." }, { heading: "Memory", body: "Page tables translate virtual pages to physical frames and the TLB caches translations. Demand paging creates minor/major faults; copy-on-write delays duplication. Working sets larger than memory cause reclaim and thrashing." }, { heading: "Files and I/O", body: "File descriptors reference open kernel objects and survive duplication/inheritance rules. User buffers, page cache, device queues, fsync, and filesystem/journal semantics determine durability. Readiness APIs avoid one blocking thread per connection but do not make CPU work asynchronous." }],
    example: { title: "Fork and copy-on-write", language: "text", code: "parent address space --fork--> shared read-only physical pages\nwrite in child ----------------> page fault + private copied page\nexec --------------------------> replace address space with new program", explanation: "Fork is initially cheap because pages are shared until written; exec is a separate replacement operation." },
    failureModes: ["Confusing process and program", "Assuming write means durable", "Too many runnable threads", "Ignoring descriptor leaks", "Treating page cache as free memory loss"],
    question: { id: "sde-os-latency", type: "case", difficulty: "hard", timeboxMinutes: 18, prompt: "A service has periodic 2-second stalls, high major faults, and rising context switches. Diagnose.", hints: ["Relate working set, memory pressure, and thread count.", "Use time-correlated OS evidence."], answer: "Correlate latency with RSS, available memory, reclaim, swap/device latency, major faults, runnable threads, and context switches. A growing working set or page-cache pressure may force disk-backed faults while thread oversubscription adds scheduling/cache cost. Identify the allocating/mapped region and thread source, bound concurrency/caches, reduce working set, and retest before changing kernel tunables.", rubric: ["Connects faults to I/O stalls", "Connects context switches to oversubscription", "Names OS measurements", "Fixes source before tuning"], tags: ["os", "latency", "debugging"] },
    flashcards: [{ front: "Process versus thread?", back: "Process owns isolated resources/address space; threads share it and have separate execution state." }, { front: "Major page fault?", back: "Required page must be fetched from storage." }, { front: "write versus fsync?", back: "write may reach cache; fsync requests persistence per filesystem/device guarantees." }],
    revision: ["Trace user/kernel transitions", "Know process/thread state", "Map virtual pages and TLB", "Track descriptor ownership", "Separate buffered write from durability"],
    sources: [{ label: "Operating Systems: Three Easy Pieces", url: "https://pages.cs.wisc.edu/~remzi/OSTEP/" }, { label: "Linux man-pages project", url: "https://www.kernel.org/doc/man-pages/" }]
  },
  "sde-systems/database-systems": {
    summary: "Reason about relational storage, indexes, transactions, MVCC, query plans, replication, and schema choices under workload.",
    durationMinutes: 95, level: "interview", roleIds: ["sde", "data-engineer", "fintech", "sre", "ml-engineer"], prerequisites: ["foundations/sql-fundamentals", "sde-systems/operating-systems"],
    outcomes: ["Explain indexes and query planning", "Reason about ACID, isolation anomalies, MVCC, and locking", "Choose schema, partitioning, and replication strategies from access patterns"],
    conceptMap: ["Schema encodes invariants", "Indexes trade write/storage cost for access paths", "Transactions order concurrent state changes", "Logs and replicas support recovery and scale"],
    keyIdea: "Database design starts with invariants and access patterns; an index or replica is never free and cannot repair an ambiguous transaction boundary.",
    analogy: { title: "A library with audited edits", body: "Indexes are catalogs, transactions are controlled edit sessions, and the log is the record used to recover the shelves." },
    theory: [{ heading: "Storage and access", body: "B-trees support ordered equality/range access; hash indexes specialize equality; LSM designs buffer writes into sorted runs. Composite-index usefulness depends on order and predicates. Optimizers estimate cardinality and choose scans, joins, and sorts—verify with actual plans." }, { heading: "Transactions", body: "Atomicity and durability depend on logging/recovery; isolation constrains anomalies such as dirty/non-repeatable reads, lost updates, write skew, and phantoms. MVCC serves snapshots from versions while locks or validation coordinate conflicting writes." }, { heading: "Scale and recovery", body: "Normalization protects update consistency; denormalization spends complexity for reads. Partitioning controls locality and pruning. Replicas add read capacity and availability but introduce lag/failover semantics. Backups require restore tests and point-in-time recovery plans." }],
    example: { title: "Atomic inventory claim", language: "sql", code: "UPDATE inventory\nSET available = available - 1\nWHERE sku = $1 AND available > 0\nRETURNING available;", explanation: "The conditional update makes check-and-decrement one atomic database statement; the enclosing order transaction still needs idempotency and failure handling." },
    failureModes: ["Indexing every column", "ORM queries without plans", "Read-modify-write outside a transaction", "Assuming replica reads are current", "Backups never restored"],
    question: { id: "sde-db-write-skew", type: "case", difficulty: "hard", timeboxMinutes: 18, prompt: "Two on-call doctors independently go off duty after each sees the other on duty. Explain and prevent the anomaly.", hints: ["Each transaction updates a different row.", "Snapshot isolation can allow write skew."], answer: "Both snapshot transactions read a state satisfying at least one doctor and update disjoint rows, so row-write conflict detection may not stop them. Enforce the invariant with serializable isolation/predicate conflict detection, lock a shared invariant row or relevant set, or redesign the constraint into a row updated by both. Retry serialization failures and test the concurrent schedule.", rubric: ["Identifies write skew", "Explains disjoint writes", "Offers valid serialization/locking redesign", "Mentions retry/test"], tags: ["dbms", "isolation", "transactions"] },
    flashcards: [{ front: "MVCC purpose?", back: "Serve consistent snapshots using versions while coordinating writes." }, { front: "Composite index order matters because?", back: "It determines which predicate/order prefixes form efficient access paths." }, { front: "Replica lag implication?", back: "Reads may not reflect completed primary writes without a consistency mechanism." }],
    revision: ["State invariants and transactions", "Choose index from query shape", "Read actual plans", "Name isolation anomalies", "Test failover and restore"],
    sources: [{ label: "PostgreSQL concurrency control", url: "https://www.postgresql.org/docs/current/mvcc.html" }, { label: "PostgreSQL index types", url: "https://www.postgresql.org/docs/current/indexes-types.html" }]
  }
};
