import type { FoundationLessonMap } from "@/data/foundations/types";

export const devopsCoreLessons: FoundationLessonMap = {
  "devops-cloud/shell-scripting": {
    summary: "Write safe, observable shell automation with predictable expansion, failure handling, pipelines, and cleanup.",
    durationMinutes: 65, level: "foundation", roleIds: ["devops", "sre", "security", "data-engineer"], prerequisites: ["foundations/linux-and-the-cli"],
    outcomes: ["Explain shell parsing, quoting, expansion, and exit status", "Build idempotent scripts with validation and cleanup", "Choose when Bash is appropriate and when to move to a general-purpose language"],
    conceptMap: ["Text is parsed then expanded", "Commands return status", "Pipelines compose streams", "Traps and checks control failure"],
    keyIdea: "Shell is an orchestration language around processes; correctness depends on controlling expansion, status, inputs, and side effects.",
    analogy: { title: "A conveyor of labeled parcels", body: "Every expansion removes or changes packaging; careful quoting and status checks ensure the right parcel reaches the next station." },
    theory: [{ heading: "Parsing and data", body: "The shell tokenizes, expands variables/commands/globs, performs word splitting where applicable, and removes quotes. Quote variable expansions, use arrays for argument lists, prefer read -r, and pass data through arguments/stdin rather than constructing commands for eval." }, { heading: "Failure and lifecycle", body: "Exit codes are the protocol. Check expected failures explicitly; strict-mode options help but have contextual edge cases. Use traps for cleanup, temporary directories with safe creation, bounded retries, timeouts, and atomic rename for durable output." }, { heading: "Automation design", body: "Validate dependencies and inputs, make operations idempotent, log actions without secrets, separate dry-run/planning from mutation, and test with lint plus disposable environments. Move to Python/Go when structured data, concurrency, complex state, or portability dominates." }],
    example: { title: "Safe temporary workspace", language: "bash", code: "#!/usr/bin/env bash\nset -Eeuo pipefail\ntmp_dir=$(mktemp -d)\ncleanup() { rm -rf -- \"$tmp_dir\"; }\ntrap cleanup EXIT INT TERM\nprintf '%s\\n' \"$payload\" >\"$tmp_dir/input\"", explanation: "The directory is resolved once, expansions are quoted, and cleanup runs across normal and interrupted exits." },
    failureModes: ["Unquoted expansions and accidental globbing", "Parsing ls output", "Secrets exposed through arguments or debug logs", "Pipelines whose early failure is ignored", "Destructive actions against unresolved variables"],
    question: { id: "devops-shell-backup", type: "case", difficulty: "medium", timeboxMinutes: 18, prompt: "Design a shell script that creates, verifies, and atomically publishes a compressed database backup.", hints: ["Separate temporary output from the final name.", "A successful compressor is not proof of a valid backup."], answer: "Validate tools, destination, permissions, and free space; create a private temporary directory; stream the database dump through compression with pipefail; capture failures without logging credentials; calculate a checksum and perform a format/restore-list verification; fsync where the durability contract requires it; then rename within the destination filesystem to publish atomically. Trap cleanup, prevent overlapping runs with a lock, emit structured status, and retain/prune only after a new verified backup exists.", rubric: ["Safe quoting and inputs", "Pipeline failure and verification", "Atomic publication and cleanup", "Locking, secrets, and retention"], tags: ["bash", "automation", "backup"] },
    flashcards: [{ front: "Why quote \"$var\"?", back: "To preserve it as one argument and prevent unintended splitting and glob expansion." }, { front: "pipefail changes?", back: "A pipeline can fail when any component fails, not only the last command." }, { front: "Idempotent script?", back: "Repeated execution converges on the intended state without unintended duplicate effects." }],
    revision: ["Quote data and use arrays", "Validate resolved targets", "Handle status intentionally", "Trap cleanup and bound retries", "Test idempotency and failure"],
    sources: [{ label: "GNU Bash manual", url: "https://www.gnu.org/software/bash/manual/bash.html" }, { label: "ShellCheck wiki", url: "https://www.shellcheck.net/wiki/" }]
  },
  "devops-cloud/linux-internals": {
    summary: "Operate Linux by connecting processes, virtual memory, filesystems, namespaces, cgroups, signals, and kernel evidence.",
    durationMinutes: 85, level: "interview", roleIds: ["devops", "sre", "security", "sde"], prerequisites: ["foundations/linux-and-the-cli", "sde-systems/operating-systems"],
    outcomes: ["Trace process, memory, filesystem, and I/O behavior", "Explain namespaces, cgroups, capabilities, and service supervision", "Diagnose saturation using kernel-visible evidence"],
    conceptMap: ["Processes consume scheduled resources", "Virtual memory maps working sets", "VFS and page cache mediate storage", "Namespaces and cgroups isolate and govern"],
    keyIdea: "Linux troubleshooting becomes systematic when every symptom is mapped to a finite resource, queue, owner, and kernel interface.",
    analogy: { title: "A managed apartment complex", body: "Namespaces change what each tenant can see, cgroups set utility budgets, and the kernel schedules shared elevators, pipes, and storage." },
    theory: [{ heading: "Execution and control", body: "Processes have credentials, namespaces, descriptors, signals, and scheduling state. fork/exec create and replace execution; PID 1 has reaping and shutdown duties. Capabilities split root privilege, while service managers define dependencies, restart policy, limits, and logging." }, { heading: "Memory and files", body: "Virtual memory, page tables, TLBs, anonymous/file-backed pages, reclaim, swap, and OOM selection explain many stalls. The VFS unifies filesystems; dentries/inodes, page cache, mounts, journaling, writeback, and fsync define visibility and durability." }, { heading: "Resource diagnosis", body: "Load average includes runnable and some uninterruptible tasks; CPU percentages need per-core and wait context. Inspect pressure, run queues, faults, RSS, slab, I/O latency/queue depth, descriptors, sockets, and cgroup throttling. Correlate before tuning sysctls." }],
    example: { title: "Container primitives", language: "text", code: "namespaces -> isolated view of PID/network/mount/user resources\ncgroups    -> accounting, limits, and throttling\ncapabilities/seccomp/LSM -> constrained privilege and syscall/access policy\nimage/rootfs -> packaged userspace", explanation: "A container is coordinated kernel mechanisms plus a runtime contract, not a miniature virtual machine." },
    failureModes: ["Treating load average as CPU utilization", "Dropping caches without identifying pressure", "SIGKILL before graceful shutdown evidence", "Running services with broad root privilege", "Tuning sysctls globally to mask application behavior"],
    question: { id: "devops-linux-load", type: "case", difficulty: "hard", timeboxMinutes: 20, prompt: "A host has low CPU utilization, load average 80, and slow requests. Investigate.", hints: ["Load includes more than running-on-CPU tasks.", "Look for a shared blocked resource."], answer: "Check runnable versus uninterruptible task states, pressure stall information, per-device I/O latency/queue depth, NFS/block errors, memory reclaim/major faults, cgroup throttling, locks, and process stacks. Identify whether many tasks wait on storage, network filesystems, kernel locks, or memory pressure. Correlate the onset with deployments and device events, isolate affected processes, restore service through bounded failover or load shedding, then fix the blocking dependency rather than adding CPU.", rubric: ["Interprets load correctly", "Inspects task/pressure/I/O evidence", "Finds shared wait cause", "Mitigates and fixes source"], tags: ["linux", "performance", "debugging"] },
    flashcards: [{ front: "Namespace versus cgroup?", back: "Namespace limits visibility/identity; cgroup accounts and controls resource use." }, { front: "D state?", back: "Uninterruptible sleep, commonly waiting in kernel I/O paths." }, { front: "Page cache role?", back: "Caches file-backed pages and buffers filesystem I/O using otherwise available memory." }],
    revision: ["Map symptom to resource and queue", "Inspect process state and pressure", "Know memory and page-cache paths", "Use namespaces/cgroups/capabilities precisely", "Tune only after correlated evidence"],
    sources: [{ label: "Linux kernel documentation", url: "https://docs.kernel.org/" }, { label: "Linux man-pages", url: "https://www.kernel.org/doc/man-pages/" }]
  },
  "devops-cloud/dns-and-tls": {
    summary: "Trace authoritative and recursive DNS plus TLS identity, key exchange, certificates, resumption, and failure.",
    durationMinutes: 80, level: "interview", roleIds: ["devops", "sre", "security", "sde"], prerequisites: ["foundations/networking-fundamentals"],
    outcomes: ["Trace recursive DNS resolution and caching", "Explain modern TLS authentication and key establishment", "Diagnose expiry, name, chain, DNSSEC, and propagation failures"],
    conceptMap: ["DNS delegates names", "Resolvers cache bounded answers", "TLS authenticates endpoints", "Handshake derives protected session keys"],
    keyIdea: "DNS answers where to connect; TLS proves who answered and protects the channel—each has independent caches, trust roots, and failure modes.",
    analogy: { title: "Directory service plus sealed introduction", body: "The directory finds an address; a chain of trusted introductions verifies the recipient before both sides agree on a private conversation key." },
    theory: [{ heading: "DNS", body: "A stub asks a recursive resolver, which follows root, TLD, and authoritative delegation unless cached. Records have TTLs; negative answers cache too. CNAME/alias behavior, split-horizon views, resolver diversity, DNSSEC validation, and stale caches shape availability." }, { heading: "TLS", body: "The client sends supported versions, ciphers, name, and key share; the server presents a certificate chain and proof, then both derive symmetric keys. Hostname/SAN validation, trust anchors, validity, revocation limitations, SNI, ALPN, forward secrecy, and session resumption matter." }, { heading: "Operations", body: "Automate certificate issuance and rotation with overlap, monitor from client perspectives, protect private keys, and test chain completeness. Lower TTL before planned DNS migration, account for cached old values, and distinguish DNS resolution, TCP reachability, TLS alert, and application failure." }],
    example: { title: "Resolution and handshake path", language: "text", code: "client -> recursive resolver -> authoritative DNS -> A/AAAA\nclient -> IP:443 (TCP/QUIC)\nClientHello(SNI, ALPN, key share)\nServerHello + certificate chain + proof\nfinished -> encrypted HTTP", explanation: "The hostname participates in both DNS lookup and TLS identity/SNI, but the two systems fail independently." },
    failureModes: ["Certificate renewed but not deployed everywhere", "Incomplete intermediate chain", "DNS cutover without TTL planning", "Wildcard certificate assumed to cover multiple label depths", "Private-key reuse across excessive blast radius"],
    question: { id: "devops-tls-some-clients", type: "case", difficulty: "hard", timeboxMinutes: 20, prompt: "A new certificate works in browsers but fails for some API clients with an unknown-authority error. Diagnose.", hints: ["Browsers may fetch or cache intermediates differently.", "Inspect the exact served chain from each edge."], answer: "Capture the handshake from affected client networks and each load-balancer/edge, verifying SNI routing, leaf SAN/validity, chain order, and that required intermediates are served. Compare client trust stores and algorithms; do not assume browser AIA fetching or cached intermediates. Check partial rollout, old instances, clock skew, and proxy interception. Deploy the complete chain consistently, preserve compatible trust/algorithm choices, validate with clean clients, and monitor certificate/chain fingerprints across endpoints.", rubric: ["Separates client and server trust behavior", "Checks served chain and SNI", "Checks partial rollout/compatibility", "Provides safe verification"], tags: ["dns", "tls", "pki"] },
    flashcards: [{ front: "Recursive versus authoritative DNS?", back: "Recursive resolvers find/cache answers; authoritative servers publish a zone's answers." }, { front: "Certificate SAN proves?", back: "The authenticated public key is valid for the named identity under a trusted chain." }, { front: "Forward secrecy?", back: "Later private-key compromise does not reveal previously captured sessions using ephemeral key exchange." }],
    revision: ["Trace resolver delegation and caches", "Validate hostname, chain, and time", "Know TLS 1.3 handshake purpose", "Rotate with overlap and monitoring", "Separate DNS, transport, TLS, and HTTP symptoms"],
    sources: [{ label: "RFC 1034 Domain Names", url: "https://www.rfc-editor.org/rfc/rfc1034" }, { label: "RFC 8446 TLS 1.3", url: "https://www.rfc-editor.org/rfc/rfc8446" }]
  },
  "devops-cloud/production-networking": {
    summary: "Design and debug production networks across routing, load balancing, NAT, firewalls, overlays, and failure domains.",
    durationMinutes: 85, level: "interview", roleIds: ["devops", "sre", "security", "sde"], prerequisites: ["devops-cloud/dns-and-tls", "sde-systems/distributed-systems-foundations"],
    outcomes: ["Trace packets across L2/L3/L4/L7 components", "Compare load-balancing, routing, NAT, and segmentation choices", "Diagnose loss, latency, MTU, asymmetric path, and exhaustion"],
    conceptMap: ["Addresses identify interfaces", "Routes choose next hops", "Stateful middleboxes transform flows", "Load balancers distribute and health-check services"],
    keyIdea: "Network debugging is path reconstruction: follow one flow in both directions and account for every lookup, translation, policy, queue, and timeout.",
    analogy: { title: "A parcel route with checkpoints", body: "Labels select roads, checkpoints rewrite or filter them, depots distribute load, and the return trip may fail if it takes a different controlled path." },
    theory: [{ heading: "Forwarding and transport", body: "Subnets and longest-prefix routing select next hops; ARP/ND resolves local neighbors. TCP provides ordered bytes with congestion/flow control; UDP/QUIC make different reliability choices. MTU and path-MTU discovery affect fragmentation and black holes." }, { heading: "Middleboxes and topology", body: "NAT tracks translated flows, firewalls apply stateless or stateful policy, and proxies/load balancers terminate or forward L4/L7 traffic. Health checks must represent readiness. Anycast/BGP, overlays, private links, and multi-zone routing add reachability and failure-domain trade-offs." }, { heading: "Diagnosis and resilience", body: "Start with name/IP/port/protocol, then inspect route, policy, connection state, packet capture, retransmission, RTT, loss, queueing, and dependency health from both ends. Watch connection/NAT/ephemeral-port exhaustion and design redundant paths whose control planes also survive." }],
    example: { title: "Connection path inventory", language: "text", code: "client -> DNS -> edge L7 LB -> firewall/NAT -> service L4 VIP\n       -> pod/VM -> database proxy -> database\nreturn path must preserve stateful devices and policy", explanation: "Writing the actual path prevents vague 'network issue' diagnosis and exposes asymmetric or exhausted state." },
    failureModes: ["One-sided connectivity checks", "Security group opened while route is absent", "Health check passes but dependency path is broken", "MTU black hole hidden by small probes", "NAT or ephemeral ports exhausted"],
    question: { id: "devops-network-large-requests", type: "case", difficulty: "hard", timeboxMinutes: 22, prompt: "Small requests succeed across a VPN but large uploads hang. Diagnose.", hints: ["Different packet sizes exercise path MTU.", "ICMP filtering can break discovery."], answer: "Reproduce with controlled packet sizes and DF behavior, inspect interface/tunnel MTUs and captures on both sides, and look for fragmentation-needed/Packet Too Big messages. Encapsulation reduces effective MTU; if ICMP is blocked, path-MTU discovery can black-hole larger packets while handshakes and small requests work. Permit required ICMP, set correct tunnel/interface MTU or TCP MSS clamping at the proper boundary, verify both directions, and monitor retransmissions rather than merely lowering all hosts blindly.", rubric: ["Identifies PMTU/encapsulation", "Uses packet-size and capture evidence", "Fixes ICMP/MTU at boundary", "Verifies both directions"], tags: ["networking", "mtu", "vpn"] },
    flashcards: [{ front: "Longest-prefix match?", back: "The most specific matching route determines the next hop." }, { front: "Stateful firewall implication?", back: "Return traffic must match tracked flow state and compatible path/policy." }, { front: "MTU black hole?", back: "Oversized packets are dropped while required size feedback is lost, so small traffic still works." }],
    revision: ["Draw both directions", "Resolve DNS, route, and policy separately", "Inspect transport loss/retransmission", "Check MTU and state-table capacity", "Test failure paths and control planes"],
    sources: [{ label: "RFC 8201 Path MTU Discovery", url: "https://www.rfc-editor.org/rfc/rfc8201" }, { label: "Cloudflare learning center networking", url: "https://www.cloudflare.com/learning/network-layer/what-is-routing/" }]
  },
  "devops-cloud/gitops": {
    summary: "Operate declarative environments through reviewed desired state, reconciliation, promotion, drift detection, and recovery.",
    durationMinutes: 65, level: "interview", roleIds: ["devops", "sre", "security", "mlops-engineer"], prerequisites: ["foundations/git-testing-and-debugging"],
    outcomes: ["Explain pull-based reconciliation and desired-state ownership", "Design promotion, policy, secrets, and rollback workflows", "Distinguish safe emergency change from unmanaged drift"],
    conceptMap: ["Git records reviewed intent", "Automation validates artifacts", "Reconcilers converge environments", "Telemetry reports health and drift"],
    keyIdea: "GitOps makes desired state auditable and continuously reconciled; Git is not a secret store, runtime database, or proof that a rollout is healthy.",
    analogy: { title: "A signed building plan with inspectors", body: "Changes enter through reviewed drawings, while inspectors continually compare the structure to the approved plan and report divergence." },
    theory: [{ heading: "Model and flow", body: "Declarative versioned state identifies what should run; an agent reconciles actual toward desired. Separate application source, immutable artifacts, environment configuration, and promotion records. Prefer digest-pinned artifacts and pull-based credentials with narrow scope." }, { heading: "Promotion and safety", body: "Promote the same artifact through environments by reviewed configuration changes. Validate schemas, policy, security, diffs, and previews before merge; gate rollout on health and support automatic or deliberate rollback. A Git revert changes desired state but recovery time depends on reconciliation and backward compatibility." }, { heading: "Secrets and operations", body: "Store references or encrypted/sealed material with controlled key management, not plaintext secrets. Detect drift, define ownership for generated fields, suspend reconciliation only through audited procedures, and reconcile emergency changes back into source. Protect repository, CI, signing, and controller identities as a supply chain." }],
    example: { title: "Artifact promotion", language: "yaml", code: "image:\n  repository: registry.example/payments\n  digest: sha256:8ab...\nrelease:\n  sourceCommit: 4d21...\n  approvedByPolicy: true", explanation: "Environments reference the same immutable artifact; promotion changes intent rather than rebuilding unknown bytes." },
    failureModes: ["Mutable image tags", "Controller credentials span every environment", "Manual hotfix never returned to Git", "Auto-sync of destructive schema changes", "Secrets committed in reversible history"],
    question: { id: "devops-gitops-emergency", type: "case", difficulty: "medium", timeboxMinutes: 18, prompt: "Design an emergency production-change process for a GitOps-managed platform.", hints: ["The responder may need speed without creating permanent drift.", "Reconciliation behavior must be explicit."], answer: "Provide a restricted break-glass role with strong authentication, time limit, approval/audit, and a narrow command set. Prefer an expedited reviewed commit; if direct mutation is necessary, pause only the affected reconciliation scope, record before/after state and incident ID, validate impact, and immediately create the matching source change or revert. Restore reconciliation, verify convergence and health, rotate exposed credentials, and review why normal delivery could not meet the recovery objective.", rubric: ["Fast but bounded access", "Explicit reconciler handling", "Drift is reconciled", "Audit, validation, and review"], tags: ["gitops", "operations", "break-glass"] },
    flashcards: [{ front: "GitOps reconciler does?", back: "Continuously compares actual state with versioned desired state and acts toward convergence." }, { front: "Rollback limitation?", back: "Reverting configuration cannot undo incompatible data or guarantee instant healthy convergence." }, { front: "Why artifact digest?", back: "It identifies immutable bytes and prevents a tag from changing underneath desired state." }],
    revision: ["Separate source, artifact, and environment state", "Promote immutable artifacts", "Validate policy and rollout health", "Protect secrets and controller identity", "Audit and reconcile emergency drift"],
    sources: [{ label: "OpenGitOps principles", url: "https://opengitops.dev/" }, { label: "CNCF GitOps working group", url: "https://github.com/cncf/tag-app-delivery/tree/main/gitops-wg" }]
  },
  "devops-cloud/ci-cd": {
    summary: "Build trustworthy delivery pipelines with reproducible artifacts, staged evidence, protected identities, and safe deployment.",
    durationMinutes: 80, level: "interview", roleIds: ["devops", "sre", "security", "sde", "mlops-engineer"], prerequisites: ["foundations/git-testing-and-debugging", "sde-systems/testing-strategy"],
    outcomes: ["Design CI stages from fast feedback to release evidence", "Harden pipeline identity, dependencies, artifacts, and provenance", "Choose deployment gates, rollout verification, rollback, and recovery"],
    conceptMap: ["Commit triggers verification", "Build produces immutable evidence", "Promotion changes environment intent", "Telemetry judges rollout health"],
    keyIdea: "A delivery pipeline is a privileged production system: it should create one verifiable artifact and progressively increase confidence without rebuilding it.",
    analogy: { title: "A controlled manufacturing line", body: "Raw material is inspected, one serialized product is tested at increasing scope, and release gates track exactly which item reaches customers." },
    theory: [{ heading: "Feedback architecture", body: "Run formatting, lint, unit, static and secret checks early; then integration, contract, migration, security, performance, and end-to-end evidence according to risk. Cache by verified inputs, keep tests hermetic where possible, and make flakiness owned work." }, { heading: "Supply-chain trust", body: "Pin and verify dependencies/actions, isolate untrusted pull requests, minimize short-lived workload identity, protect runners and secrets, generate SBOM/provenance, sign artifacts, and separate build from deployment authority. Never expose protected credentials to fork-controlled code." }, { heading: "Delivery", body: "Promote one immutable artifact through environments. Use progressive exposure, readiness gates, database-compatible sequencing, observability, approval where risk warrants, and automated rollback only for safe reversible conditions. Record who, what, evidence, and environment." }],
    example: { title: "Progressive pipeline", language: "text", code: "commit -> lint/unit/SAST -> reproducible build + SBOM + signature\n       -> integration/migration tests -> staging smoke\n       -> 1% canary -> SLO/guardrail evaluation -> gradual rollout", explanation: "Evidence attaches to the same artifact while exposure and confidence grow." },
    failureModes: ["Rebuilding separately per environment", "Long-lived cloud keys in CI", "Forked code reaches secrets", "Green deployment gate ignores user SLOs", "Rollback attempted after incompatible schema contraction"],
    question: { id: "devops-cicd-payments", type: "system-design", difficulty: "hard", timeboxMinutes: 28, prompt: "Design CI/CD for a payment service with database migrations and strict auditability.", hints: ["Use expand-migrate-contract sequencing.", "Separate build identity from production deployment identity."], answer: "On reviewed commits run fast checks, dependency/secret/SAST scans, unit/property tests, then build once in an isolated runner with pinned inputs, SBOM, provenance, signature, and immutable digest. Integration-test real database and payment contracts. Use expand schema, deploy backward-compatible code, resumably backfill with checks, then contract later. Promote the signed digest via short-lived environment-scoped identity and approval policy, canary on payment SLIs and ledger invariants, record evidence/actor, and maintain tested rollback/roll-forward and break-glass procedures.", rubric: ["One verifiable artifact", "Migration compatibility", "Least-privilege supply chain", "Canary/audit/recovery"], tags: ["ci-cd", "payments", "supply-chain"] },
    flashcards: [{ front: "Build once, promote means?", back: "The identical immutable artifact advances; environment configuration changes without rebuilding code." }, { front: "Why short-lived workload identity?", back: "It reduces secret theft lifetime and binds access to a specific trusted pipeline context." }, { front: "Expand-migrate-contract?", back: "Add compatible schema, move data/code safely, then remove old shape after all consumers migrate." }],
    revision: ["Optimize early feedback", "Pin inputs and isolate builds", "Sign artifact, SBOM, and provenance", "Promote with scoped identity", "Gate on user health and compatible rollback"],
    sources: [{ label: "SLSA specification", url: "https://slsa.dev/spec/" }, { label: "NIST Secure Software Development Framework", url: "https://csrc.nist.gov/Projects/ssdf" }]
  }
};
