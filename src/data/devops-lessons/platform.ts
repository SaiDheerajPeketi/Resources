import type { FoundationLessonMap } from "@/data/foundations/types";

export const platformLessons: FoundationLessonMap = {
  "devops-cloud/containers": {
    summary: "Understand images, runtimes, namespaces, cgroups, filesystems, registries, and the security boundary containers do and do not provide.",
    durationMinutes: 80, level: "foundation", roleIds: ["devops", "sre", "security", "sde", "mlops-engineer"], prerequisites: ["devops-cloud/linux-internals"],
    outcomes: ["Trace image build, distribution, unpack, and process startup", "Explain namespace, cgroup, capability, filesystem, and network isolation", "Build minimal, reproducible, non-root, operable images"],
    conceptMap: ["Image layers package a root filesystem", "Runtime config creates isolated processes", "Registry distributes content-addressed artifacts", "Kernel policy limits resource and privilege"],
    keyIdea: "A container is a Linux process with packaged userspace and constrained views/resources; it shares the host kernel and is not automatically a security sandbox.",
    analogy: { title: "A furnished room in a shared building", body: "The room has its own contents and locks, but utilities and the structural foundation are still shared with the building." },
    theory: [{ heading: "Image and runtime", body: "OCI images are content-addressed manifests and filesystem layers plus configuration. A runtime unpacks layers, prepares namespaces/cgroups/mounts/network, applies security options, and execs the entrypoint. Copy-on-write layers reduce transfer but writable container state is ephemeral." }, { heading: "Build and distribution", body: "Use deterministic pinned bases, multi-stage builds, cache mounts, a narrow build context, and non-root final images. Scan and sign by digest, generate SBOM/provenance, and enforce registry retention without deleting live references. Architecture/platform must match nodes." }, { heading: "Operations and isolation", body: "Set CPU/memory requests and limits with awareness of throttling/OOM, forward signals, reap children, expose health intentionally, write logs to streams, and persist data externally. Drop capabilities, use seccomp/LSMs, read-only filesystems, and stronger sandbox/VM isolation for hostile multi-tenancy." }],
    example: { title: "Minimal multi-stage image", language: "dockerfile", code: "FROM golang:1.25@sha256:... AS build\nWORKDIR /src\nCOPY . .\nRUN CGO_ENABLED=0 go build -trimpath -o /out/app ./cmd/app\nFROM gcr.io/distroless/static-debian12:nonroot@sha256:...\nCOPY --from=build /out/app /app\nUSER nonroot:nonroot\nENTRYPOINT [\"/app\"]", explanation: "Build tools stay outside the runtime image; digests and a non-root minimal base improve reproducibility and reduce attack surface." },
    failureModes: ["Mutable latest tags", "Secrets copied into layers", "PID 1 ignores signal/reaping behavior", "Running privileged or as root by default", "State written only to the writable layer"],
    question: { id: "devops-container-oom", type: "case", difficulty: "hard", timeboxMinutes: 20, prompt: "A container is repeatedly OOM-killed while host memory looks available. Diagnose.", hints: ["The cgroup limit can be lower than host capacity.", "Inspect working set, cache, and runtime memory policy."], answer: "Confirm the container/cgroup termination reason and limit, then inspect current/peak memory, anonymous versus file cache, allocation/heap profile, process children, and node pressure. A cgroup can invoke OOM despite free host memory. Check request/limit mismatch, language heap sizing unaware of the limit, leaks/unbounded queues, and temporary peaks. Bound workload/concurrency, configure runtime memory targets, set evidence-based requests/limits with headroom, and verify under realistic load rather than simply removing the limit.", rubric: ["Recognizes cgroup OOM", "Collects memory breakdown and profiles", "Finds workload/runtime cause", "Sizes and retests safely"], tags: ["containers", "cgroups", "memory"] },
    flashcards: [{ front: "Image versus container?", back: "Image is immutable packaged content/config; container is a runtime process instance with writable state." }, { front: "Why digest pin?", back: "It selects exact immutable content rather than a mutable tag." }, { front: "Container isolation shares?", back: "The host kernel, so privilege and kernel attack surface still matter." }],
    revision: ["Trace OCI image to process", "Build minimal and reproducible", "Handle PID 1 and signals", "Set resources from measurements", "Drop privilege and externalize state"],
    sources: [{ label: "OCI image specification", url: "https://github.com/opencontainers/image-spec" }, { label: "Kubernetes containers concepts", url: "https://kubernetes.io/docs/concepts/containers/" }]
  },
  "devops-cloud/kubernetes-operations": {
    summary: "Operate Kubernetes scheduling, networking, storage, rollouts, autoscaling, upgrades, and failures from control-plane evidence.",
    durationMinutes: 95, level: "interview", roleIds: ["devops", "sre", "security", "mlops-engineer", "data-engineer"], prerequisites: ["devops-cloud/kubernetes-reconciliation", "devops-cloud/containers", "devops-cloud/production-networking"],
    outcomes: ["Trace workload scheduling, readiness, service routing, and storage binding", "Debug pending, crashing, unavailable, and throttled workloads", "Plan capacity, autoscaling, disruption, upgrade, and disaster recovery"],
    conceptMap: ["API stores desired objects", "Controllers create dependent resources", "Scheduler binds Pods to nodes", "Kubelets and data planes realize execution"],
    keyIdea: "Kubernetes automates convergence, not intent quality: operators must read conditions and events along the object dependency chain.",
    analogy: { title: "An airport operations system", body: "Schedules declare desired flights, controllers prepare resources, dispatch assigns gates, and local crews report actual readiness and disruptions." },
    theory: [{ heading: "Workload path", body: "Deployments manage ReplicaSets and Pods; the scheduler filters/scores nodes from requests, constraints, taints, topology, and capacity; kubelets ask the runtime to start containers. Readiness gates endpoints, Services route stable virtual access, and Ingress/Gateway adds external policy." }, { heading: "State and scaling", body: "Volumes and claims bind storage with access, topology, reclaim, snapshot, and expansion semantics. Requests drive scheduling and HPA utilization; limits can throttle or OOM. HPA, VPA, and node autoscaling form feedback loops with delay, so stabilization and headroom matter." }, { heading: "Operations", body: "Use probes for their distinct purposes, disruption budgets for voluntary disruption, anti-affinity/topology spread for failure domains, and rollout surge/unavailable controls. Upgrade control plane/nodes/add-ons compatibly, back up cluster state and application data separately, and inspect conditions/events/logs/metrics before deleting evidence." }],
    example: { title: "Pending Pod decision chain", language: "text", code: "Pod Pending\n -> scheduler event: no feasible nodes\n -> compare requests, taints/tolerations, affinity, PVC topology, quota\n -> inspect autoscaler decision\n -> change constraint/capacity; do not repeatedly recreate", explanation: "The event and object graph identify why reconciliation cannot reach a scheduled, ready workload." },
    failureModes: ["CPU requests omitted so scheduling is fictional", "Liveness probe restarts a slow but recoverable service", "PDB mistaken for protection from node failure", "Autoscalers fight each other", "etcd snapshot assumed to back up application volumes"],
    question: { id: "devops-k8s-pending", type: "case", difficulty: "hard", timeboxMinutes: 24, prompt: "After a rollout, half the Pods remain Pending and the autoscaler does not add nodes. Investigate.", hints: ["Read scheduler and autoscaler reasons.", "A constraint can make every possible node infeasible."], answer: "Inspect Pod conditions/events and scheduler messages for request, taint, affinity, topology, host-port, quota, and PVC conflicts. Compare the pending Pod template with node-group shapes and autoscaler logs: scale-up is skipped when no configured node type could satisfy constraints or a hard limit/quota is reached. Verify requests, required anti-affinity, zones allowed by the storage class, taints/tolerations, max node group size, and cloud quota. Correct the minimal constraint/capacity issue, then watch scheduling, readiness, rollout budget, and saturation.", rubric: ["Uses object conditions/events", "Checks scheduler constraints", "Explains autoscaler feasibility", "Verifies recovery end to end"], tags: ["kubernetes", "scheduling", "autoscaling"] },
    flashcards: [{ front: "Request versus limit?", back: "Request guides scheduling/reservation; limit enforces a runtime ceiling or throttle." }, { front: "Readiness versus liveness?", back: "Readiness controls traffic eligibility; liveness requests restart when the process cannot recover." }, { front: "PDB covers?", back: "Limits simultaneous voluntary disruptions; it does not prevent involuntary failures." }],
    revision: ["Follow object dependency chain", "Read conditions and events first", "Align requests, topology, and autoscaling", "Separate readiness, liveness, and startup", "Plan upgrades and data recovery"],
    sources: [{ label: "Kubernetes concepts", url: "https://kubernetes.io/docs/concepts/" }, { label: "Kubernetes cluster administration", url: "https://kubernetes.io/docs/concepts/cluster-administration/" }]
  },
  "devops-cloud/infrastructure-as-code": {
    summary: "Manage infrastructure through declarative models, dependency graphs, immutable artifacts, policy, state, and controlled reconciliation.",
    durationMinutes: 75, level: "interview", roleIds: ["devops", "sre", "security", "data-engineer"], prerequisites: ["devops-cloud/gitops", "devops-cloud/ci-cd"],
    outcomes: ["Compare declarative and imperative infrastructure automation", "Design reusable modules, environments, state, and promotion", "Control drift, destructive change, secrets, policy, and recovery"],
    conceptMap: ["Configuration declares desired resources", "Graph orders dependencies", "State maps intent to real objects", "Plan and policy govern change"],
    keyIdea: "Infrastructure as code is lifecycle management with an auditable change model; source files alone do not capture state, provider behavior, or external mutation.",
    analogy: { title: "A continuously reconciled land registry", body: "Blueprints state intent, the registry maps named parcels to real assets, and every proposed transfer is reviewed before execution." },
    theory: [{ heading: "Model and lifecycle", body: "Declarative tools calculate differences and dependency order; imperative tools express procedure and may suit bootstrapping or orchestration. Resource identity, create/update/delete semantics, replacement, eventual consistency, and provider/API versions shape safe plans." }, { heading: "Structure and state", body: "Build small cohesive modules with typed inputs, constrained outputs, versioning, and ownership. Separate blast radius and credentials through environment/state boundaries. Protect remote state with encryption, locking, versioning, access logs, and recovery; never hand-edit it casually." }, { heading: "Delivery and governance", body: "Format/validate/lint/test modules, scan policy/security/cost, review a fresh plan, and apply the reviewed artifact with scoped identity. Detect drift, import legitimate existing resources, protect critical deletion, sequence migrations, and keep break-glass changes auditable and reconciled." }],
    example: { title: "Plan as evidence", language: "text", code: "commit -> validate/module tests -> provider lock verification\n       -> speculative plan + policy/cost checks\nreview -> approved saved plan -> scoped apply -> postcondition checks", explanation: "The applied change should match reviewed inputs and provider versions rather than an unreviewed re-plan." },
    failureModes: ["One state file for the entire company", "Apply permissions available to pull requests", "Provider versions float", "Sensitive outputs exposed in logs/state", "Destructive replacement hidden in a large plan"],
    question: { id: "devops-iac-drift", type: "case", difficulty: "hard", timeboxMinutes: 22, prompt: "A critical database was manually resized during an incident and now IaC plans to shrink and replace it. Respond safely.", hints: ["Do not blindly apply or delete the state entry.", "Decide whether the emergency change should become desired state."], answer: "Freeze the affected apply, preserve the plan and incident evidence, and compare configuration, state, provider read, and actual database. Determine whether resize is the new desired state; update code and import/refresh state using documented procedures, with backups and deletion protection. Investigate why replacement is proposed, provider schema/version changes, and shrink feasibility. Review a narrow plan, apply only after data/recovery checks, then restore drift monitoring and improve the break-glass reconciliation process.", rubric: ["Stops unsafe apply", "Triangulates config/state/actual", "Codifies or deliberately reverses change", "Protects data and improves process"], tags: ["iac", "drift", "database"] },
    flashcards: [{ front: "IaC state purpose?", back: "Maps configuration identities and attributes to real resources for lifecycle and diff calculation." }, { front: "Drift?", back: "Actual infrastructure differs from the declared/recorded desired state." }, { front: "Module quality?", back: "Cohesive responsibility, typed contract, versioning, tests, and clear ownership—not maximal reuse." }],
    revision: ["Know resource identity and lifecycle", "Partition state by blast radius", "Pin tools/providers/modules", "Review exact plans with policy", "Protect deletion, secrets, drift, and recovery"],
    sources: [{ label: "HashiCorp infrastructure as code", url: "https://developer.hashicorp.com/terraform/intro" }, { label: "OpenTofu documentation", url: "https://opentofu.org/docs/" }]
  },
  "devops-cloud/terraform": {
    summary: "Use Terraform safely across configuration, graph evaluation, state, modules, providers, plans, imports, and refactors.",
    durationMinutes: 85, level: "interview", roleIds: ["devops", "sre", "security", "data-engineer"], prerequisites: ["devops-cloud/infrastructure-as-code"],
    outcomes: ["Explain Terraform evaluation, graph, plan, apply, and state", "Design maintainable modules and environment boundaries", "Perform safe imports, moves, replacement, and provider upgrades"],
    conceptMap: ["HCL expressions build configuration", "Providers expose resource schemas", "Graph orders operations", "State binds addresses to remote objects"],
    keyIdea: "Terraform resource addresses and state are part of the lifecycle contract; refactoring code can mutate infrastructure unless identity moves are declared.",
    analogy: { title: "A labeled wiring diagram", body: "Changing a label without recording that the same wire moved can make the electrician remove one circuit and build another." },
    theory: [{ heading: "Evaluation and graph", body: "Variables, locals, data sources, resources, modules, count/for_each, expressions, and references produce a dependency graph. Unknown values defer decisions to apply. Explicit depends_on is for hidden dependencies, not a substitute for correct data flow." }, { heading: "State and planning", body: "State records resource addresses, remote IDs, attributes, and lineage. Remote backends need locking, encryption, versioning, and scoped access. Refresh reads providers; plan proposes actions; apply must handle eventual consistency and partial failure. Sensitive marks redact display, not storage." }, { heading: "Evolution", body: "Prefer for_each with stable keys over positional count for identity. Use moved/import blocks and state commands with backups for refactors/adoption. Pin Terraform/provider/module versions, inspect upgrade schemas and plans, use lifecycle guards sparingly, and avoid provisioners when a native resource or image build exists." }],
    example: { title: "Stable resource keys", language: "hcl", code: "resource \"aws_iam_user\" \"team\" {\n  for_each = toset(var.usernames)\n  name     = each.key\n}\n\nmoved {\n  from = aws_iam_user.users\n  to   = aws_iam_user.team\n}", explanation: "Stable keys avoid index-shift replacements; a moved block records identity during refactoring." },
    failureModes: ["count indexes shift after deletion", "Sensitive values assumed absent from state", "Local state used by a team", "Targeted apply becomes normal workflow", "Provider major upgrade applied with no saved plan"],
    question: { id: "devops-terraform-module-refactor", type: "case", difficulty: "hard", timeboxMinutes: 24, prompt: "Refactor 100 production resources into a module without recreation.", hints: ["Configuration address changes must be mapped.", "Work in a recoverable, reviewable sequence."], answer: "Back up and lock the remote state, pin current tool/providers, and ensure the baseline plan is empty. Create the module with equivalent arguments and use moved blocks from every old address to its new stable module address, or carefully scripted state mv where necessary. Test on a representative environment, inspect a full plan for zero create/destroy, apply the address-only transition with scoped access, verify remote objects, then make functional changes separately. Preserve rollback/state versions and avoid count-to-for_each identity changes without explicit key mapping.", rubric: ["Baseline and state protection", "Explicit address mapping", "Zero-destruction plan", "Staged verification and rollback"], tags: ["terraform", "state", "refactor"] },
    flashcards: [{ front: "Terraform state stores?", back: "Address-to-remote identity and attributes used to plan and manage lifecycle." }, { front: "for_each advantage?", back: "Stable meaningful keys avoid positional identity shifts." }, { front: "Sensitive value flag guarantees?", back: "Display redaction; the value can still be present in state and must be protected." }],
    revision: ["Understand addresses and graph", "Secure/lock/version remote state", "Use stable for_each keys", "Declare moves and imports", "Pin versions and review exact plans"],
    sources: [{ label: "Terraform language documentation", url: "https://developer.hashicorp.com/terraform/language" }, { label: "Terraform state documentation", url: "https://developer.hashicorp.com/terraform/language/state" }]
  },
  "devops-cloud/configuration-and-secrets": {
    summary: "Deliver configuration and secrets with typed contracts, least privilege, rotation, audit, and failure-safe rollout.",
    durationMinutes: 75, level: "interview", roleIds: ["devops", "sre", "security", "sde", "mlops-engineer"], prerequisites: ["devops-cloud/infrastructure-as-code", "cybersecurity/identity-and-access-management"],
    outcomes: ["Separate code, configuration, feature control, and secrets", "Design secret issuance, delivery, rotation, and revocation", "Roll configuration changes safely across versions and failure domains"],
    conceptMap: ["Schemas define configuration contract", "Distribution targets environments", "Identity authorizes secret retrieval", "Rotation changes value without breaking consumers"],
    keyIdea: "A secret is a time-bounded capability, not a special environment variable; its complete lifecycle includes creation, delivery, use, rotation, revocation, and audit.",
    analogy: { title: "A hotel keycard system", body: "Access is issued to an identity for a room and time, delivered privately, rotated without replacing the building, and revoked with an audit trail." },
    theory: [{ heading: "Configuration contracts", body: "Treat config as typed versioned input with defaults, validation, ownership, and compatibility. Separate deploy-time settings, dynamic feature flags, and per-request policy. Decide whether a change needs restart or hot reload and make partial rollout observable." }, { heading: "Secret lifecycle", body: "Prefer short-lived dynamically issued credentials from workload identity. Central stores encrypt and audit secrets, while applications retrieve through authenticated channels or agents. Minimize scope and copies; avoid source, image layers, logs, crash dumps, broad environment inheritance, and client bundles." }, { heading: "Rotation and failure", body: "Use overlapping validity or multi-key verification, version references, cache expiry, and coordinated producer/consumer sequencing. Test revoked/expired/unavailable secret behavior. Fail closed for authorization while preserving appropriate availability through cached time-bounded credentials and break-glass controls." }],
    example: { title: "Overlap rotation", language: "text", code: "1. issue credential v2; server accepts v1 + v2\n2. distribute reference/version v2; observe adoption\n3. stop new v1 use; revoke v1\n4. verify no consumers and remove fallback", explanation: "Overlap avoids a flag day while telemetry proves every consumer migrated before revocation." },
    failureModes: ["Secrets baked into images", "One shared credential across workloads", "Rotation without dual-read overlap", "Configuration reload partially applies", "Secret value exposed in logs or IaC state"],
    question: { id: "devops-secret-rotation", type: "system-design", difficulty: "hard", timeboxMinutes: 22, prompt: "Design zero-downtime rotation for database credentials used by hundreds of service instances.", hints: ["Avoid a single instant when all clients and server must switch.", "Prefer identity-issued short-lived access if supported."], answer: "Prefer workload identity with dynamically issued short-lived database credentials. Otherwise create v2 with least privilege, make the database accept v1 and v2, publish a versioned secret reference through authenticated delivery, and have clients refresh pools without dropping in-flight work. Measure adoption by credential version, block new v1 sessions after all healthy instances migrate, drain old sessions, then revoke v1. Handle store outage with bounded cached credentials, redact telemetry, audit access, and maintain an emergency revocation path.", rubric: ["Short-lived/least-privilege model", "Overlap and pool refresh", "Adoption evidence before revoke", "Outage, audit, and emergency behavior"], tags: ["secrets", "rotation", "database"] },
    flashcards: [{ front: "Secret versus config?", back: "A secret grants capability and needs confidentiality, scope, rotation, revocation, and audit." }, { front: "Dynamic secret advantage?", back: "Short lifetime and identity binding reduce shared static credential exposure." }, { front: "Why version config?", back: "Producers and consumers can coordinate compatibility and diagnose which value is active." }],
    revision: ["Type and validate configuration", "Use workload identity and short lifetimes", "Minimize secret copies and scope", "Rotate with overlap and evidence", "Test expiry, revocation, and store outage"],
    sources: [{ label: "NIST Digital Identity Guidelines", url: "https://pages.nist.gov/800-63-4/" }, { label: "Kubernetes secrets good practices", url: "https://kubernetes.io/docs/concepts/security/secrets-good-practices/" }]
  },
  "devops-cloud/release-strategies": {
    summary: "Choose rolling, blue-green, canary, feature-flag, and shadow techniques from risk, compatibility, cost, and recovery needs.",
    durationMinutes: 70, level: "interview", roleIds: ["devops", "sre", "sde", "security", "mlops-engineer"], prerequisites: ["devops-cloud/ci-cd", "devops-cloud/kubernetes-operations"],
    outcomes: ["Compare deployment and release strategies", "Design automated guardrails and progressive exposure", "Plan data, API, queue, and rollback compatibility"],
    conceptMap: ["Deployment changes software", "Release changes user exposure", "Guardrails evaluate health", "Compatibility determines reversibility"],
    keyIdea: "Progressive delivery reduces blast radius only when traffic segmentation, health evidence, and a safe stop or recovery path are real.",
    analogy: { title: "Opening a bridge lane by lane", body: "Inspectors expose limited traffic, compare structural signals, and can halt expansion before every traveler is committed." },
    theory: [{ heading: "Strategies", body: "Rolling replaces instances gradually with mixed-version compatibility. Blue-green switches between full environments for rapid traffic rollback at higher cost. Canary exposes a cohort progressively. Feature flags decouple code deployment from behavior release; shadowing duplicates traffic without user-visible responses but can still cause side effects." }, { heading: "Guardrails", body: "Choose representative cohorts and compare latency, errors, saturation, business/security/quality metrics, and SLO burn against a stable baseline. Use minimum sample/time, abort thresholds, manual judgment for ambiguous changes, and observability by version and cohort." }, { heading: "Compatibility and recovery", body: "Backward/forward-compatible APIs, events, database schema, caches, and jobs enable mixed versions. Rollback may be unsafe after data mutation; prefer expand-migrate-contract, reversible flags, and roll-forward. Test draining, long-lived connections, queues, regional sequencing, and failed rollout cleanup." }],
    example: { title: "Canary exposure", language: "text", code: "1% internal/low-risk -> evaluate fast errors\n5% one zone -> compare SLO + business invariants\n25% multi-zone -> soak + capacity\n50% -> 100% -> retain rollback window", explanation: "Stages and metrics depend on workload; explicit hold and abort criteria matter more than these percentages." },
    failureModes: ["Canary receives no representative traffic", "Rollback ignores database or event incompatibility", "Feature flags have no owner or expiry", "Shadow requests trigger real side effects", "Global rollout outruns monitoring"],
    question: { id: "devops-release-schema", type: "case", difficulty: "hard", timeboxMinutes: 24, prompt: "Release a service that splits one database column into two while old and new instances coexist.", hints: ["Schema change and behavior exposure need separate stages.", "Plan reads, writes, backfill, and contraction."], answer: "Expand by adding nullable/new columns and indexes without removing the old field. Deploy compatible code that can read old/new and writes both or uses a transition policy, then backfill resumably with rate limits and verification. Canary new reads/behavior by cohort while watching correctness and database load. After all writers/readers are migrated and reconciliation proves equality, stop old writes, retain a rollback window, then contract the old column in a later release. Document event/cache compatibility too.", rubric: ["Expand-migrate-contract", "Mixed-version read/write semantics", "Progressive evidence and backfill safety", "Delayed contraction and rollback window"], tags: ["release", "database", "canary"] },
    flashcards: [{ front: "Deploy versus release?", back: "Deploy installs code; release exposes behavior to users or traffic." }, { front: "Blue-green strength?", back: "Fast traffic switch between full environments, at added cost and data compatibility constraints." }, { front: "Canary guardrail needs?", back: "Representative cohort, baseline, enough evidence, thresholds, and an actionable abort path." }],
    revision: ["Separate deployment from exposure", "Choose cohort and guardrails", "Preserve mixed-version compatibility", "Plan data/event rollback limits", "Clean flags and failed rollout state"],
    sources: [{ label: "Google SRE: canarying releases", url: "https://sre.google/workbook/canarying-releases/" }, { label: "Kubernetes Deployments", url: "https://kubernetes.io/docs/concepts/workloads/controllers/deployment/" }]
  }
};
