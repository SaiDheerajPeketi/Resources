import type { TechnologyDepth } from "@/data/technology-depth";

export const languageTechnologyDepth: Record<string, TechnologyDepth> = {
  cpp: {
    learningPath: [
      { level: "basic", title: "Values and control flow", objective: "Write small C++17 programs and explain every object's type, value, and lifetime.", topics: ["fundamental types, conversions, expressions, and control flow", "functions, overloads, namespaces, headers, and translation units", "classes, constructors, destructors, const, references, and value semantics", "strings, vectors, arrays, iterators, and standard algorithms", "compile, link, warning, sanitizer, and debugger workflow"] },
      { level: "intermediate", title: "Ownership and generic programming", objective: "Build exception-safe components with explicit ownership and standard-library idioms.", topics: ["RAII, stack lifetime, unique_ptr, shared_ptr, weak_ptr, and ownership graphs", "copy/move construction, assignment, rule of zero/five, and exception guarantees", "templates, deduction, specialization, concepts, and constexpr", "STL containers, algorithms, ranges, comparators, and invalidation", "errors, exceptions, filesystem, streams, CMake, and unit testing"] },
      { level: "advanced", title: "The abstract machine", objective: "Reason about layout, dispatch, optimization, undefined behavior, and concurrency.", topics: ["storage duration, object lifetime, alignment, padding, aliasing, and placement", "virtual dispatch, object slicing, multiple inheritance, and ABI boundaries", "compilation model, linkage, ODR, templates, and modules", "undefined, unspecified, and implementation-defined behavior", "threads, atomics, locks, condition variables, futures, and memory order"] },
      { level: "expert", title: "Production systems judgment", objective: "Design, measure, and diagnose reliable low-level C++ under realistic constraints.", topics: ["profiling, cache locality, branch behavior, allocation, vectorization, and benchmarking", "allocators, arenas, intrusive structures, zero-copy, and ownership APIs", "async I/O, cancellation, backpressure, and low-latency trade-offs", "sanitizers, fuzzing, static analysis, crash dumps, and postmortem debugging", "API/ABI compatibility, dependency hygiene, secure parsing, and staged modernization"] }
    ],
    setup: ["Install a current Clang or GCC toolchain and verify the compiler, linker, standard library, debugger, and CMake are compatible.", "Compile interview code as C++17 with -Wall -Wextra -Wpedantic and explain every warning.", "Maintain separate debug/sanitized and optimized builds because instrumentation and optimization expose different failures.", "Use an out-of-source CMake build and compile_commands.json so editors, analysis, and CI share flags.", "Run one unit test, one AddressSanitizer/UndefinedBehaviorSanitizer check, and one debugger session before relying on the environment."],
    runtime: ["Each translation unit is preprocessed and compiled to object code; the linker resolves definitions, symbols, templates, libraries, and startup.", "Automatic objects normally end at scope exit, static objects span program or thread lifetime, and dynamic storage ends when its owner releases it.", "The abstract machine defines observable behavior while compilers may transform any implementation that preserves defined behavior.", "Value semantics copy or move state; references and pointers create aliases whose validity depends on the target lifetime.", "Threads share an address space, and conflicting unsynchronized accesses to ordinary memory create a data race and undefined behavior."],
    testing: ["Test public behavior, ownership transfer, boundaries, invalid inputs, and exception guarantees rather than private helper shape.", "Run ASan/UBSan for memory and undefined behavior, TSan for races in a compatible build, and static analysis for missed paths.", "Use property tests and fuzzers for parsers, serializers, containers, and state machines with strong invariants.", "Use a benchmark harness and prevent dead-code elimination, warm-up bias, and unrealistic data layout."],
    debugging: ["Start with the first compiler diagnostic; later template errors are often consequences of the earliest failed requirement.", "Use a debugger for stacks, values, memory, threads, and watchpoints, then core dumps for non-interactive failures.", "Enable sanitizers before manually tracing corruption because the visible crash may occur far after the invalid access.", "Inspect assembly or optimization remarks only after a profiler identifies the hot path.", "For hangs, collect every thread stack, lock owner, wait predicate, queue depth, and shutdown state."],
    performance: ["Choose algorithms and data layout before micro-optimizing syntax; contiguous storage often wins through locality.", "Measure wall time, CPU, allocation, cache misses, branch misses, context switches, and tail latency separately.", "Prefer value types and reserve predictable capacity, but do not trade correctness for speculative allocation removal.", "Treat copies, moves, virtual calls, exceptions, bounds checks, and abstractions as costs to measure rather than slogans to eliminate."],
    security: ["Parse untrusted lengths, indexes, integer conversions, encodings, paths, and binary formats with bounds and overflow checks.", "Use RAII containers and spans instead of raw owning pointers and unchecked pointer-length pairs at interfaces.", "Compile production targets with applicable hardening, sanitizers in testing, dependency scanning, and strong warnings.", "Avoid shell construction, unsafe format strings, lifetime escapes, uninitialized data, and serialization of raw object layout."],
    failureModes: ["A pointer, reference, iterator, or string_view outlives its object or is invalidated by container mutation.", "Double ownership, shared_ptr cycles, manual new/delete, or mismatched allocation APIs leak or corrupt memory.", "Signed overflow, out-of-bounds access, invalid shifts, uninitialized reads, or data races invoke undefined behavior.", "An exception crosses a destructor, C ABI, thread entry, or noexcept boundary without an intentional policy.", "Header definitions violate the One Definition Rule or library changes break ABI despite source compatibility.", "A micro-optimization worsens locality, prediction, compile time, or maintainability without improving the measured workload."],
    theorySections: [
      { title: "Values, objects, references, and lifetimes", explanation: "C++ distinguishes storage, object lifetime, value, and expressions used to identify objects. A reference is an alias and a pointer is an address-like value; neither inherently owns the target. Storage can exist before an object's lifetime begins or after it ends, so a non-null pointer can still dangle. Value categories guide overload resolution and moves but do not themselves promise that a move occurs.", keyPoints: ["Trace object lifetime separately from storage duration.", "References alias; raw pointers own only by documented convention.", "A moved-from object remains valid under its specified but often unspecified state.", "string_view and span are non-owning views whose backing storage must outlive them."] },
      { title: "RAII and deterministic resource management", explanation: "Resource Acquisition Is Initialization binds successful acquisition to an object whose destructor releases the resource. Stack unwinding composes cleanup across early returns and exceptions. The rule of zero delegates ownership to members that already implement correct copy, move, and destruction. unique_ptr expresses exclusive dynamic ownership, shared_ptr counts shared ownership, and weak_ptr observes without extending lifetime.", keyPoints: ["Prefer value members and the rule of zero.", "Use unique ownership unless shared lifetime is genuine.", "shared_ptr controls lifetime, not every invariant inside the object.", "Destructors should not emit failures that escape during unwinding."] },
      { title: "Templates, concepts, and compile-time polymorphism", explanation: "Templates are instantiated for concrete arguments after name lookup and deduction select a candidate. Substitution failure can remove candidates, while concepts state required operations with clearer constraints. Compile-time polymorphism can inline and specialize behavior without virtual dispatch, but it increases generated code, build work, and coupling to implementation headers.", keyPoints: ["Template definitions generally must be visible at instantiation.", "Concepts constrain interfaces but cannot prove every semantic law.", "Perfect forwarding preserves value categories only through its specific pattern.", "Use runtime polymorphism when hiding implementation or runtime substitution matters."] },
      { title: "Containers, iterators, and algorithmic cost", explanation: "Standard containers expose different complexity, layout, ordering, ownership, and invalidation guarantees. vector stores elements contiguously and usually gives excellent locality; node-based containers preserve selected references but add allocation and indirection. Iterators express traversal capabilities, while algorithms separate operations from storage. Big-O must be combined with data size and hardware behavior.", keyPoints: ["Know invalidation rules for every mutation used.", "reserve changes capacity, not logical size.", "Unordered containers provide expected rather than universal constant-time lookup.", "Prefer algorithms that make boundaries and intent explicit without hiding complexity."] },
      { title: "Undefined behavior and the abstract machine", explanation: "Undefined behavior is not a recoverable runtime category: the standard imposes no requirements on an execution that contains it, and optimizers may reason that such paths cannot occur. Unspecified behavior chooses among allowed alternatives, while implementation-defined behavior must be documented. Sanitizers detect many violations but cannot prove the absence of undefined behavior.", keyPoints: ["The crash site may be far from the original violation.", "Signed overflow is undefined; unsigned arithmetic wraps modulo its range.", "Strict aliasing and alignment constrain how stored bytes are accessed as objects.", "Compiler success and stable output do not make undefined behavior safe."] },
      { title: "Concurrency and the memory model", explanation: "The C++ memory model defines sequenced-before within a thread and inter-thread happens-before through synchronization. Atomics prevent data races for their objects and offer ordering from relaxed through sequential consistency. Mutex release/acquire also creates ordering. A condition variable is a notification mechanism; the protected predicate remains the truth and must be checked in a loop.", keyPoints: ["A data race on ordinary memory is undefined behavior.", "Atomicity of one variable does not protect a multi-field invariant.", "Relaxed atomics add no cross-object synchronization.", "Deadlock, starvation, livelock, and false sharing are distinct failures."] }
    ],
    workedExamples: [
      { title: "Exclusive ownership with automatic cleanup", language: "C++17", code: "class File {\n  std::FILE* handle_;\npublic:\n  explicit File(const char* path) : handle_(std::fopen(path, \"rb\")) {\n    if (!handle_) throw std::system_error(errno, std::generic_category());\n  }\n  ~File() { std::fclose(handle_); }\n  File(const File&) = delete;\n  File& operator=(const File&) = delete;\n};", explanation: "Construction either produces a valid owner or throws. Destruction closes the file on every scope exit, and copying is forbidden because two owners would double-close one handle." },
      { title: "Avoid iterator invalidation during growth", language: "C++17", code: "std::vector<int> values{1, 2, 3};\nvalues.reserve(6);\nauto first = values.begin();\nvalues.push_back(4);\nstd::cout << *first; // valid here, but not after reallocation", explanation: "A vector reallocation moves elements and invalidates every reference, pointer, and iterator. reserve protects only the known capacity window; validity still follows documented mutations." },
      { title: "Publish immutable data with release/acquire", language: "C++17", code: "std::string message;\nstd::atomic<bool> ready{false};\n\nmessage = \"complete\";\nready.store(true, std::memory_order_release);\n\nif (ready.load(std::memory_order_acquire)) {\n  std::cout << message;\n}", explanation: "When the acquire load observes the release store, the earlier message write happens-before the read. This is a publication pattern, not a general replacement for protecting mutable state." }
    ],
    misconceptions: [
      { claim: "C++ passes objects by reference by default.", correction: "Value parameters copy or move; only reference or pointer parameters alias caller-visible state.", whyItHappens: "Optimized copies and class syntax hide the selected copy/move operations." },
      { claim: "std::move moves an object.", correction: "std::move is a cast that permits move-aware overload selection; the selected operation decides what transfers.", whyItHappens: "The helper's name describes intended use rather than its literal operation." },
      { claim: "Smart pointers make ownership automatically correct.", correction: "They encode selected lifetime policies, but shared cycles, wrong ownership, aliases, and concurrent invariants still require design.", whyItHappens: "Automatic destruction is confused with correct ownership architecture." },
      { claim: "A list is faster than a vector for insertion.", correction: "Only insertion after a known node is constant time; traversal, allocation, and poor locality often make vector faster.", whyItHappens: "One asymptotic mutation step is quoted while lookup and hardware costs are omitted." },
      { claim: "Undefined behavior means a throw or crash at that line.", correction: "No behavior is required, and optimization may change code far before or after the apparent violation.", whyItHappens: "Debug builds appear deterministic and make UB look like a normal runtime error." },
      { claim: "An atomic member makes the containing object thread-safe.", correction: "Atomics protect their own operations; compound relationships across fields still need one synchronization policy.", whyItHappens: "Atomic access is confused with preservation of a larger invariant." }
    ],
    revisionChecklist: ["Trace automatic, static, thread, and dynamic storage duration against actual object lifetime.", "Explain value categories, copy elision, move construction, and the valid moved-from rule.", "Choose raw reference/pointer, unique_ptr, shared_ptr, weak_ptr, span, or value ownership deliberately.", "State vector, deque, list, map, and unordered_map complexity plus invalidation and locality trade-offs.", "Explain compilation, linking, name mangling, ODR, header templates, and ABI risk.", "Distinguish undefined, unspecified, and implementation-defined behavior with examples.", "Construct happens-before through mutexes or atomics and identify a data race.", "Use compiler diagnostics, debugger, sanitizers, profiler, and benchmark harness for distinct evidence.", "Give basic, strong, and no-throw exception guarantees and design RAII cleanup.", "Explain when zero-cost abstraction claims hold and how measurement can disprove an assumption."],
    questions: [
      { prompt: "What does std::move do, and what state is the source left in?", difficulty: "medium", answer: "std::move casts to an xvalue so overload resolution can select a move operation; it moves nothing itself. A standard-library moved-from object is valid but generally has an unspecified value unless documented otherwise.", whyTricky: "The name suggests a transfer and candidates often say the source becomes empty or invalid.", rubric: ["Calls std::move a cast.", "Makes the selected operation responsible for transfer.", "States valid but usually unspecified source state."] },
      { prompt: "Differentiate pointer validity, object lifetime, and storage duration.", difficulty: "hard", answer: "Storage is memory with a duration; an object's lifetime begins and ends within suitable storage; a pointer may retain the same address after lifetime ends but dereferencing it as the former object is invalid. Placement construction makes the distinction explicit.", whyTricky: "An address is treated as proof that a live object exists there.", rubric: ["Separates storage from object lifetime.", "Explains dangling non-null pointers.", "Uses a local-return or placement example."] },
      { prompt: "When does vector invalidate iterators and why can reserve help?", difficulty: "medium", answer: "Reallocation invalidates all iterators, references, and pointers. Insertion without reallocation has position-specific invalidation. reserve guarantees a capacity floor but never changes size or gives permanent stability.", whyTricky: "Candidates memorize that push_back invalidates without conditioning it on capacity.", rubric: ["Names reallocation behavior.", "Distinguishes size from capacity.", "Avoids permanent-stability claims."] },
      { prompt: "Why is returning string_view to a local string wrong?", difficulty: "easy", answer: "string_view is a non-owning pointer-length view. The local string destroys its backing characters at return, so the view dangles. Return an owning string or view caller-owned/static storage with an explicit lifetime.", whyTricky: "Value-like syntax and cheap copying hide the borrowed lifetime.", rubric: ["Identifies non-ownership.", "Connects destruction to dangling.", "Offers a safe alternative."] },
      { prompt: "Explain the strong exception guarantee and copy-and-swap.", difficulty: "hard", answer: "The strong guarantee leaves observable state unchanged on failure. Prepare new state in temporary RAII owners, then commit with non-throwing swap. It may cost extra allocation or lose storage reuse, so it is not universal.", whyTricky: "Exception safety is confused with catching exceptions.", rubric: ["Defines unchanged-on-failure.", "Explains prepare then commit.", "Mentions RAII and cost."] },
      { prompt: "What is the difference between a data race and a race condition?", difficulty: "hard", answer: "A data race is conflicting unsynchronized access to one memory location with at least one write; in C++ it is undefined behavior. A race condition is broader timing-dependent incorrectness and can occur with atomics, such as check-then-act.", whyTricky: "The terms are used interchangeably in casual discussion.", rubric: ["Gives the formal data-race rule.", "States undefined behavior.", "Provides a non-data-race race condition."] },
      { prompt: "When would you choose virtual dispatch over templates?", difficulty: "medium", answer: "Use virtual dispatch for runtime substitution, stable interfaces, implementation hiding, or bounded code size. Use templates for compile-time customization and value-type specialization. Include ABI, build time, diagnostics, ownership, and known type-set trade-offs.", whyTricky: "The choice is reduced to one indirect call versus inlining.", rubric: ["Uses runtime versus compile-time variation.", "Mentions ABI/build/code-size.", "Avoids universal speed claims."] },
      { prompt: "Why can signed overflow break a bounds check?", difficulty: "hard", answer: "Signed overflow is undefined, so an optimizer may assume it never occurs when proving relationships and remove or reorder checks. Validate before arithmetic or use checked operations; use unsigned only for intentional modular semantics.", whyTricky: "Two's-complement hardware is mistaken for the portable language rule.", rubric: ["States signed overflow is undefined.", "Connects assumptions to optimization.", "Proposes checked arithmetic."] }
    ],
    flashcards: [
      { front: "RAII", back: "Own a resource in an object; construction establishes it and destruction releases it on every scope exit." },
      { front: "std::move", back: "A cast to an xvalue that enables move-aware overload selection; it performs no move itself." },
      { front: "Rule of zero", back: "Compose ownership from members whose copy, move, and destruction already express the policy." },
      { front: "Dangling view", back: "A non-owning pointer/reference/span/string_view whose backing object lifetime ended." },
      { front: "Vector reallocation", back: "Moves storage and invalidates every pointer, reference, and iterator into the old allocation." },
      { front: "Undefined behavior", back: "An execution for which the standard imposes no requirements; not a defined crash mode." },
      { front: "Data race", back: "Conflicting unsynchronized accesses to one location, at least one a write; undefined behavior." },
      { front: "Acquire/release", back: "When acquire observes release, prior producer actions happen-before later consumer actions." }
    ],
    cheatsheet: [
      { title: "Ownership", items: [{ label: "Value", value: "own directly" }, { label: "unique_ptr", value: "exclusive dynamic owner" }, { label: "shared_ptr", value: "shared lifetime, cycle risk" }, { label: "span/view", value: "borrow; owner outlives" }] },
      { title: "Special members", items: [{ label: "Zero", value: "prefer owning members" }, { label: "Copy", value: "duplicate by policy" }, { label: "Move", value: "transfer; source valid" }, { label: "Destructor", value: "release; do not leak failure" }] },
      { title: "Container choice", items: [{ label: "vector", value: "default; contiguous + local" }, { label: "deque", value: "ends + segmented storage" }, { label: "map", value: "ordered O(log n)" }, { label: "unordered", value: "expected O(1), hash policy" }] },
      { title: "Evidence", items: [{ label: "Warnings", value: "compile-time defects" }, { label: "Sanitizers", value: "UB/memory/race evidence" }, { label: "Debugger", value: "state + stacks + memory" }, { label: "Profiler", value: "measured hot resource" }] }
    ]
  },
  python: {
    learningPath: [
      { level: "basic", title: "Readable Python", objective: "Write small correct programs and explain binding, mutability, control flow, and errors.", topics: ["names, objects, built-in types, identity, equality, truth, and mutability", "expressions, control flow, functions, arguments, scopes, and closures", "lists, tuples, dictionaries, sets, comprehensions, sorting, and complexity", "exceptions, files, pathlib, context managers, modules, and standard I/O", "virtual environments, modules, scripts, formatting, typing, and pytest"] },
      { level: "intermediate", title: "Pythonic composition", objective: "Use the data model and protocols to build testable packages with explicit boundaries.", topics: ["classes, dataclasses, properties, descriptors, inheritance, composition, and MRO", "iterables, iterators, generators, decorators, callables, and context managers", "typing, protocols, generics, unions, narrowing, and static-checker limits", "packaging, pyproject.toml, dependencies, imports, entry points, and environments", "testing, fixtures, boundary mocks, property tests, logging, and debugging"] },
      { level: "advanced", title: "Runtime and concurrency", objective: "Reason about CPython execution, memory, async I/O, and production performance.", topics: ["bytecode, frames, namespaces, descriptors, attribute lookup, and metaclasses", "reference counting, cyclic GC, weak references, finalization, and memory profiling", "GIL, threads, processes, asyncio tasks, event loop, cancellation, and backpressure", "serialization, multiprocessing boundaries, native extensions, and interpreter differences", "profiling CPU, allocation, I/O, database work, and algorithmic bottlenecks"] },
      { level: "expert", title: "Production Python systems", objective: "Design, operate, and evolve secure Python services and data systems under load.", topics: ["API and schema compatibility, migrations, dependency locks, and release policy", "worker models, queue capacity, timeouts, retries, idempotency, and graceful shutdown", "secure parsing, subprocesses, templates, paths, secrets, and dependency provenance", "observability, tracing, structured logs, crash diagnosis, and leak analysis", "performance escape hatches, vectorization, compiled extensions, and architecture trade-offs"] }
    ],
    setup: ["Install a supported Python 3 release and verify python and pip refer to the intended interpreter rather than an operating-system Python.", "Create a project-local virtual environment and use python -m pip or a locked project tool so installation targets are unambiguous.", "Declare metadata and dependencies in pyproject.toml, separate runtime from development tools, and commit a reproducible lock where supported.", "Run modules with python -m package.module to preserve package import semantics and keep executable side effects behind an entry point.", "Configure formatting, linting, type checking, unit tests, and coverage as explicit repeatable commands before adding application code."],
    runtime: ["Source is parsed and compiled to bytecode that an interpreter executes through frames containing code, global, built-in, and local state.", "Names are bound to objects; assignment rebinds a name, while mutation changes an object that several names can reference.", "CPython primarily uses reference counting plus cyclic garbage collection, but critical resources need context managers rather than collection timing.", "Attribute access follows the data model through instance state, class MRO, and descriptors; special methods implement protocols.", "In common CPython builds the GIL serializes Python bytecode per interpreter, while I/O, processes, and releasing native code provide other concurrency paths."],
    testing: ["Test pure policy with ordinary values, then integration-test database, filesystem, serialization, network, and framework boundaries realistically.", "Use fixtures for explicit setup and cleanup and avoid global monkeypatching that makes dependencies invisible.", "Test iterators, context managers, async cancellation, retries, timeouts, partial failures, and cleanup—not only returned values.", "Use property-based tests for parsers, transformations, financial invariants, and algorithms with broad input spaces."],
    debugging: ["Read a traceback from the final exception through the first relevant application frame and preserve chained causes.", "Use breakpoint(), pdb, structured logs, and minimal reproductions to find the first wrong binding or transition.", "Use faulthandler and thread/task dumps for hangs; inspect pending tasks, blocking calls, pools, and queues.", "Use cProfile or sampling profilers for CPU, tracemalloc for allocation, and database/network traces for external waits.", "Inspect sys.path, module.__file__, the environment, and installed metadata when imports differ across machines."],
    performance: ["Choose the algorithm and built-in operation first; Python-level loops magnify interpreter overhead while native operations batch work.", "Separate CPU, blocking I/O, event-loop delay, serialization, allocation, garbage collection, database queries, and queue waits.", "Use generators to bound memory for streams, but measure repeated computation and I/O lifetime introduced by laziness.", "Use threads for suitable I/O, processes or native/vectorized code for CPU parallelism, and async only with non-blocking cancellation-aware dependencies."],
    security: ["Never eval or exec untrusted input; treat pickle-like object deserialization as code execution and use safe data formats where trust is absent.", "Use parameterized queries, safe templates, allow-listed paths and commands, and argument arrays instead of shell-interpolated subprocess strings.", "Keep secrets outside source and logs, use least-privilege identities, and validate uploaded size, type, name, and destination.", "Pin and review dependencies, verify build sources, minimize transitive packages, and patch supported runtimes promptly."],
    failureModes: ["A mutable default argument or late-bound closure shares state or reads a final loop value unexpectedly.", "A shallow copy duplicates only the outer container, leaving nested mutable objects shared.", "Import cycles expose partially initialized modules or a local file shadows a standard or third-party module.", "Blocking CPU or synchronous I/O inside an async task stalls every task on the event loop.", "Unbounded tasks, process pools, queues, retries, or response buffering exhaust memory and downstream capacity.", "Catching Exception too broadly hides cancellation, context, programmer errors, or a partial external side effect that needs reconciliation."],
    theorySections: [
      { title: "Names, objects, identity, and mutability", explanation: "Python assignment binds a name to an object; it does not declare a storage slot containing an object value. Multiple names can reference one mutable object, so mutation is visible through every alias. Rebinding changes only one namespace entry. Identity asks whether two references designate the same object, while equality invokes value-defined behavior. Function arguments use call-by-sharing: an object reference is passed by value.", keyPoints: ["Use is for identity and singleton sentinels such as None; use == for value equality.", "Immutable objects cannot change, but names can be rebound.", "Shallow copies preserve nested aliases; deep copying follows a configurable graph.", "Default argument objects are created once when the function is defined."] },
      { title: "Protocols, data model, and duck typing", explanation: "Python maps syntax to protocols such as iteration, containment, context management, numeric operations, hashing, calling, and attribute access. Duck typing depends on supported behavior rather than declared ancestry. Abstract base classes and typing.Protocol can document or statically check expectations, but semantic correctness still depends on laws such as stable hashing and iterator behavior.", keyPoints: ["Special methods are usually looked up on the type, not as ordinary instance attributes.", "Descriptors power methods, properties, slots, and framework behavior.", "Equality without a compatible stable hash makes objects unsafe or unhashable.", "Composition and small protocols reduce coupling to wide hierarchies."] },
      { title: "Iteration, generators, and lazy evaluation", explanation: "An iterable can create an iterator; an iterator returns successive values and raises StopIteration when exhausted. A generator function creates an iterator whose frame is suspended at yield, retaining locals and control position. Laziness reduces peak memory and supports infinite streams, but it defers work and failure, ties resources to consumption, and can repeat expensive computation when recreated.", keyPoints: ["Iterators are usually single-pass; iterables may create fresh iterators.", "yield from delegates values, exceptions, and completion behavior.", "Generator cleanup matters when a suspended frame owns files, transactions, or locks.", "A list comprehension is eager; a generator expression is lazy."] },
      { title: "Exceptions, context managers, and resource ownership", explanation: "Exceptions unwind frames until a matching handler is found, executing finally blocks and context-manager exits along the way. raise from records causality between failures. A context manager establishes a resource or state transition in __enter__ and guarantees corresponding __exit__ handling. It may suppress an exception only by deliberately returning true.", keyPoints: ["Catch the narrowest exception where recovery or added meaning is possible.", "Use else for work that should run only when try succeeds.", "Do not rely on __del__ or reference-count timing for critical resources.", "Async context managers pair acquisition and cleanup that require awaiting."] },
      { title: "Imports, packaging, and environments", explanation: "Import resolves a qualified name along the search path, creates a module object, executes its top-level code once, and caches it in sys.modules. Packages organize import namespaces, while installed distributions provide them and may have different names. Virtual environments isolate interpreter-visible installations, and lock plus metadata policy makes resolution reproducible.", keyPoints: ["Circular imports observe partially initialized modules.", "Absolute imports and a src layout reduce accidental shadowing.", "python -m selects the intended interpreter for pip and module execution.", "A lock captures resolution but does not replace compatibility testing."] },
      { title: "Concurrency, async I/O, and the GIL", explanation: "Threads share memory and suit blocking I/O or native calls that release the GIL. Processes isolate memory and can execute Python CPU work in parallel but pay serialization and startup costs. asyncio schedules cooperative tasks on an event loop; progress depends on awaiting non-blocking operations. Cancellation is control flow that must propagate through finally blocks and cleanup.", keyPoints: ["The GIL is not an application thread-safety guarantee.", "Async is concurrency, not automatic CPU parallelism.", "A blocking call on the event-loop thread stalls unrelated tasks.", "Bound task creation, queues, pools, and retries and propagate deadlines."] }
    ],
    workedExamples: [
      { title: "Avoid shared mutable default arguments", language: "Python", code: "def append_tag(tag: str, tags: list[str] | None = None) -> list[str]:\n    owned = [] if tags is None else tags\n    owned.append(tag)\n    return owned\n\nassert append_tag(\"a\") == [\"a\"]\nassert append_tag(\"b\") == [\"b\"]", explanation: "None is an immutable sentinel. A new list is allocated for each omitted argument, while a caller-supplied list is intentionally mutated and returned." },
      { title: "Stream records with bounded memory", language: "Python", code: "def valid_rows(lines):\n    for line in lines:\n        name, raw_score = line.rstrip(\"\\n\").split(\",\", 1)\n        score = int(raw_score)\n        if 0 <= score <= 100:\n            yield name, score\n\nwith open(\"scores.csv\", encoding=\"utf-8\") as source:\n    top = sum(score >= 90 for _, score in valid_rows(source))", explanation: "The file and generator are consumed inside one context so the resource outlives lazy iteration. Only the current line and tuple need memory." },
      { title: "Bound async work and preserve cancellation", language: "Python", code: "async def fetch_all(urls):\n    limit = asyncio.Semaphore(20)\n    async def one(url):\n        async with limit:\n            async with asyncio.timeout(2):\n                return await client.get_bytes(url)\n    async with asyncio.TaskGroup() as group:\n        tasks = [group.create_task(one(url)) for url in urls]\n    return [task.result() for task in tasks]", explanation: "The semaphore bounds downstream concurrency, timeout gives each operation a deadline, and TaskGroup owns child lifetimes so failure cancels siblings and awaits cleanup." }
    ],
    misconceptions: [
      { claim: "Python variables store values directly.", correction: "Names bind to objects; assignment rebinds a name and mutation changes a referenced mutable object.", whyItHappens: "Simple immutable examples make rebinding look like copying a value into a box." },
      { claim: "Python passes mutable arguments by reference and immutable ones by value.", correction: "All arguments pass object references by value under call-by-sharing; mutability only changes what aliases can observe.", whyItHappens: "List mutation is visible while rebinding an integer parameter is not." },
      { claim: "The GIL makes Python code thread-safe.", correction: "The GIL protects interpreter internals in common CPython builds; multi-step application invariants still race and I/O can interleave.", whyItHappens: "Serialized bytecode is confused with atomic workflows." },
      { claim: "async functions automatically run in parallel.", correction: "Calling an async function creates a coroutine; tasks make cooperative progress when awaited operations yield.", whyItHappens: "Concurrent syntax is mistaken for extra CPU resources." },
      { claim: "A generator is simply a memory-efficient list.", correction: "It is a stateful single-pass computation whose work, failures, and resource use occur during consumption.", whyItHappens: "Both can be iterated with identical for syntax." },
      { claim: "Type hints enforce runtime types.", correction: "Annotations are metadata used by tools and frameworks; Python normally does not reject incompatible arguments at function entry.", whyItHappens: "Static checker diagnostics resemble compiler errors from enforced type systems." }
    ],
    revisionChecklist: ["Explain name binding, identity, equality, mutability, aliasing, shallow copy, and call-by-sharing.", "Trace LEGB lookup, closure cells, late binding, decorators, and nonlocal/global behavior.", "Use iteration, context-manager, callable, comparison, hash, and descriptor protocols deliberately.", "Distinguish iterable, iterator, generator function, generator object, comprehension, and generator expression.", "Explain exception propagation, chaining, finally, context cleanup, and narrow recovery boundaries.", "Diagnose imports through package context, sys.path, sys.modules, metadata, and environment selection.", "Choose threads, processes, asyncio, vectorization, or native code from the workload and ownership model.", "Explain the GIL without claiming application thread safety or universal lack of parallelism.", "Use pytest fixtures, property tests, debugger, profiler, tracemalloc, and task/thread dumps for distinct evidence.", "Secure serialization, queries, templates, subprocesses, paths, secrets, uploads, and dependencies."],
    questions: [
      { prompt: "Is Python pass-by-value or pass-by-reference? Demonstrate the real model.", difficulty: "easy", answer: "Python uses call-by-sharing: an object reference is passed by value. The parameter initially names the same object. Mutating that object is visible through aliases, while rebinding the parameter changes only the local name.", whyTricky: "Candidates invent separate rules for lists and integers from observed mutability.", rubric: ["States one call-by-sharing rule.", "Separates mutation from rebinding.", "Uses one mutable example."] },
      { prompt: "Why is a mutable default argument shared across calls?", difficulty: "easy", answer: "Default expressions run once when def executes, and the object is stored with the function. Each omitted argument binds to that object. Use an immutable sentinel such as None and create per-call mutable state inside.", whyTricky: "The default appears in the signature and is assumed to run at call time.", rubric: ["Names definition-time evaluation.", "Explains the shared stored object.", "Shows the sentinel pattern."] },
      { prompt: "Differentiate __getattr__, __getattribute__, properties, and descriptors.", difficulty: "hard", answer: "__getattribute__ participates in every instance lookup; __getattr__ is a fallback. property is a data descriptor. Descriptors define __get__, __set__, or __delete__ and participate in precedence with instance state and MRO; functions are descriptors that bind methods.", whyTricky: "Framework magic is memorized without the lookup order.", rubric: ["Gets always-versus-fallback right.", "Identifies property as a descriptor.", "Mentions precedence or method binding."] },
      { prompt: "What happens when a generator yields, and when is finally run?", difficulty: "hard", answer: "yield returns a value and suspends the frame with its position and locals. It resumes on next, send, or throw. finally runs on completion, exception, explicit close, or finalization, but critical resources need explicit owning contexts.", whyTricky: "Generators are described as list replacements, hiding suspended frames and cleanup lifetime.", rubric: ["Explains frame suspension.", "Names resume mechanisms.", "Connects close/finally to resource ownership."] },
      { prompt: "Why does CPU-heavy work block asyncio, and how should it be handled?", difficulty: "medium", answer: "The event loop runs task continuations until they await. CPU-heavy Python code does not yield, so timers and sockets wait. Reduce or chunk work, use processes for Python CPU tasks, or use appropriate vectorized/native code.", whyTricky: "async syntax is confused with preemptive scheduling and parallelism.", rubric: ["Explains cooperative scheduling.", "Identifies non-yielding CPU work.", "Chooses an appropriate escape path."] },
      { prompt: "Does the GIL remove the need for locks?", difficulty: "hard", answer: "No. The GIL is an interpreter mechanism, not an application invariant lock. Workflows span bytecodes, invoke user code, release during I/O/native work, and interleave between check and action. Use locks, queues, immutability, confinement, transactions, or redesign.", whyTricky: "Some built-in operations appear atomic in one CPython version and are generalized.", rubric: ["Rejects application thread-safety.", "Explains multi-step interleaving.", "Names valid alternatives."] },
      { prompt: "Why can a circular import expose a missing attribute?", difficulty: "medium", answer: "Python inserts a module in sys.modules before finishing its top-level code. In a cycle, the peer receives that partially initialized object, so names defined later are absent. Move shared contracts or reduce import-time coupling.", whyTricky: "Import is viewed as textual inclusion rather than cached execution.", rubric: ["Mentions early sys.modules insertion.", "Explains partial initialization.", "Proposes a structural remedy."] },
      { prompt: "When is shallow copy correct, and why can deepcopy be dangerous?", difficulty: "medium", answer: "Shallow copy is correct when shared children are intended or immutable. deepcopy may duplicate identity-bearing resources, explode cost, mishandle handles, or violate domain ownership. Prefer explicit domain copying of state that should diverge.", whyTricky: "Copy depth is treated as a more-is-safer setting.", rubric: ["Explains nested alias preservation.", "Names resource and identity risks.", "Recommends ownership-aware copying."] }
    ],
    flashcards: [
      { front: "Python assignment", back: "Bind a name to an object; it neither copies the object nor declares fixed value storage." },
      { front: "call-by-sharing", back: "Pass an object reference by value; mutation is shared, parameter rebinding is local." },
      { front: "Mutable default", back: "Created once when def executes; use a sentinel for per-call mutable state." },
      { front: "Iterable versus iterator", back: "An iterable creates an iterator; an iterator carries next-position state and is usually single-pass." },
      { front: "Generator", back: "An iterator backed by a suspended frame containing control position and locals." },
      { front: "Context manager", back: "Paired setup and cleanup that runs exit handling on success or exception." },
      { front: "Import cache", back: "sys.modules stores module objects, including partially initialized modules during cycles." },
      { front: "asyncio", back: "Cooperative task scheduling; progress requires non-blocking operations that await or yield." }
    ],
    cheatsheet: [
      { title: "Object model", items: [{ label: "Name", value: "binding to object" }, { label: "is", value: "identity" }, { label: "==", value: "value protocol" }, { label: "Mutation", value: "visible through aliases" }] },
      { title: "Protocols", items: [{ label: "Iterate", value: "__iter__ + __next__" }, { label: "Context", value: "__enter__ + __exit__" }, { label: "Attribute", value: "MRO + descriptors + instance" }, { label: "Hash", value: "equal ⇒ same stable hash" }] },
      { title: "Concurrency", items: [{ label: "Thread", value: "shared memory; I/O/native work" }, { label: "Process", value: "parallel CPU; serialization cost" }, { label: "asyncio", value: "cooperative I/O tasks" }, { label: "Rule", value: "bound work + propagate cancellation" }] },
      { title: "Diagnosis", items: [{ label: "Correctness", value: "traceback + pdb + minimal case" }, { label: "CPU", value: "cProfile/sampling profiler" }, { label: "Memory", value: "tracemalloc + object roots" }, { label: "Hang", value: "thread/task stacks + queues" }] }
    ]
  }
};
